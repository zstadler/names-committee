# פרויקט: מיצוי נתוני ועדת השמות הממשלתית מתוך ילקוט הפרסומים

## ⚠️ הודעה למופע-חדש של Claude שפותח את המסמך הזה - קרא/י את זה קודם

**עדכון 9.9.2026 (מאוחר יותר באותו יום): הקוד נבנה מחדש והצינור עובד - ראו "גרסה 26" בסוף
המסמך.** הסעיף הזה (אובדן-הקוד) נשמר כתיעוד-היסטורי של הרקע; **צעדי-השחזור שהוא מציע כבר
בוצעו**. הקבצים הקיימים כעת: `ocr_parser.py`, `ocr_parser2.py`, `run_batch_600dpi`,
`build_v3_output`, `samples/booklet_2633.pdf`, `samples/booklet_3165.pdf`. **לפני כל שינוי,
קרא/י את "גרסה 26"** - היא מתעדת מנגנונים שהשתנו מהמפרט שבגוף המסמך.

**נכון ל-9.9.2026: כל קבצי-הקוד-בפועל אבדו** (איפוס-סביבת-עבודה, `/home/claude/reshumot`
כולה נעלמה - קוד, קובצי-PDF-לדוגמה, נתוני-scan, הכל). **המסמך הזה (PROJECT_NOTES.md) הוא
המקור-האמין-היחיד שנותר** לשחזור הפרויקט - הוא מתעד **מפרט-מלא** (לא רק כוונות) של איך כל
מנגנון אמור לעבוד במצבו-הסופי-המאומת, כולל ערכי-סף מדויקים, תבניות-regex, וארכיטקטורה.

**נבדק ונשלל:** ניסינו לשחזר קוד-מדויק מתדפיס-PDF-של-השיחה-הקודמת (670 עמ') - **לא עבד**.
תוכן-כלים (תוצאות `view`/קוד-מלא) לא נשמר בשכבת-הטקסט של ה-PDF בכלל (0 התאמות למחרוזות-
קוד-ייחודיות שידוע-בוודאות שהופיעו). רק הפרוזה-השיחתית שרדה, וגם היא עם בעיות-כיווניות-RTL.
**אל תבזבז/י זמן על נסיון-חוזר בגישה הזו** - זה נבדק ישירות, לא הונח.

**המלצה מפורשת מהשיחה הקודמת: לבנות את הקוד מחדש** (לא לנסות "לשחזר-במדויק" 25 סבבי-עריכה) -
מונחה-מפרט מהסעיפים למטה, עם אימות-אמפירי מול התוצאות-הידועות (12/12 מועצות אזוריות, 11
קטגוריות, 501 רשומות בפלט הסופי, ומקרים-ספציפיים-מתועדים כמו השלמות-קואורדינטה "1572.9336").

**צעדים ראשונים מוצעים:** (1) הורדת-מחדש של קובצי-ה-PDF-לדוגמה - הכתובות-המדויקות-לכל-גיליון
נמצאות ב-`stage1_input_clean.xlsx` (עמודת "קישור ישיר לעמוד ההודעה") ששרד גם הוא באותה תיקייה;
דפוס-השרת התקין מתועד למטה (`pub-justice.openapi.gov.il`, לא `free-justice`). (2) בניית
`ocr_parser.py`/`ocr_parser2.py` לפי הארכיטקטורה המתועדת בגוף המסמך (לא בנספח - הנספח
היסטורי-בלבד). (3) אימות מול התוצאות למעלה.

### ⚠️ פער-תיעוד ידוע - קרא/י גם את זה
בסבב-העבודה **האחרון לפני אובדן-הסביבה** (ביקורת-מנגנונים שיטתית, ראה `MECHANISMS_AUDIT.md`),
**כמה מנגנונים נוספים הוסרו מהקוד אך מעולם לא תועדו כ"גרסה 26"** (השיחה נקטעה לפני שהספקתי):
`merge_prefix_lines` (0 פגיעות ב-50 טורים - התברר שהמיזוג-הכללי-יותר ב-`group_words_to_lines`
כבר מכסה את אותו מקרה), **`is_name_line` הוסרה כליל** (לא רק ה-fallback-שלה, כל הפונקציה -
0 קריאות אחרי המעבר למכונת-המצבים מבוססת-הרווח), וכתוצאה-משרשור: `rescue_near_miss_names`
(תוצאתה כבר לא נצרכה ע"י איש), `NON_NAME_ADMIN_TOKEN_RE` (0 פגיעות ב-50 טורים בקריאה-החיה
היחידה שנשארה, בתוך `split_self_contained_entry`), `PURE_NUMERIC_LINE_RE` ו-
`DESCRIPTION_OPENER_RE` (יתומות ברגע ש-`is_name_line`/`rescue_near_miss_names` הוסרו).
**המנגנונים האלה מוזכרים במסמך הזה (בסעיף "ארכיטקטורת הפרסר הנוכחית" ובנספח) כאילו הם
עדיין קיימים/פעילים - זה לא מעודכן.** אל תשחזר/י אותם.

**חשוב באותה מידה:** אחרי ההסרות האלה בוצעה **רק בדיקת-שפיות-נקודתית על עמוד 13 בודד**,
**לא הרצה-מלאה-מחדש על כל 25 העמודים**. המספרים "12/12 מועצות, 501 רשומות" למעלה **משקפים
את המצב לפני ההסרות-האחרונות-האלה** (מ"גרסה 25"), לא מאומתים-מחדש אחריהן. **כשתשחזר/י ותריץ
את הקוד, ההרצה-המלאה-הראשונה שלך היא גם ההזדמנות-הראשונה לאמת שההסרות האלה לא שינו את
התוצאה** - זה עדיין לא נבדק.

### שיטת-העבודה שלנו לאורך הפרויקט - חשוב להבין את ההיקף
**כל העבודה המתועדת במסמך הזה מתמקדת בגיליון 2633 בלבד** (פורמט "1969-1992"). הרחבה
לגיליונות-אחרים (277, 971, 3165, 4012) **הייתה נקודתית-ומוגבלת** - אך ורק להשוואת-מבנה
(דו-טורי/חד-טורי/טבלה) ובדיקת-יציבות-סף-קליברציה בין 2633 ל-3165 (ראו "גרסה 24") - **מעולם
לא הרצנו את הפרסר-המלא על אף גיליון מלבד 2633**, וגם 2633 עצמו **טרם עבר כיול/כיוונון-סופי
לתוצאות-מיטביות** - כל הגרסאות (1-25) הן שיפורים-מצטברים שכל אחד תיקן בעיה-ספציפית-שנמצאה,
לא סבב-כיוונון-שיטתי-סופי. סביר שיש עוד בעיות-לא-מתועדות בגיליון 2633 עצמו, ובוודאי שהצינור
עדיין לא מוכן להרצה-אמיתית על 41 הגיליונות הנותרים בלי בדיקה נוספת בכל אחד.

---

**מסמך זה מתאר את מצב הפרויקט הנוכחי.** כל התהליך הכרונולוגי (אבחונים, ניסיונות שנכשלו,
דיוני-בדיקה מפורטים) מרוכז ב**נספח א'** בסוף המסמך - לעיון ולהבנת הרציונל מאחורי כל החלטה,
אך אינו נדרש להבנת המצב הנוכחי.

## מטרת הפרויקט
חילוץ **כל** ההחלטות של ועדת השמות הממשלתית (1950-2026) מתוך גיליונות ילקוט הפרסומים
בפירוט מלא (לא רק זיהוי אילו גיליונות רלוונטיים - זוהה כבר בשלב קודם, 42 גיליונות "אמיתיים").
בהמשך: השוואה לטופונומסטיקון (ספר השמות הגיאוגרפיים 1997, 232 עמ', מצורף - **טרם התחלנו שלב זה**).

## קלט: רשימת 42 הפרסומים
קובץ: `stage1_input_clean.xlsx` (טאב "קלט שלב 1 - נקי").
כל שורה: תאריך, מספר גיליון, עמוד תחילת ההודעה, סה"כ עמודים, קישור ישיר.
33 רשומות נוספות סוננו (אינדקס/מפתח, הודעות מינוי, לא-רלוונטי) - מתועדות בטאב השני לביקורת.

## דורות פורמט - סטטוס
| # | פורמט | דוגמה | סטטוס |
|---|---|---|---|
| 1 | 1969-1992 (עשיר, 2 עמודות, מנוקד) | גיליון 2633 | **בעבודה - פרסר עובד, ראה למטה** |
| 2 | 1953-1958 (מינימלי, טבלת קואורדינטות) | גיליון 277 | לא התחלנו |
| 3 | 1959-1968 (בינוני, חד-עמודי) | גיליון 971 | לא התחלנו |
| 4 | 2006-2026 (מודרני, טקסט דיגיטלי נקי) | גיליון 14581 | לא התחלנו (צפוי הכי-פשוט, אין OCR) |

כל 4 התקופות שוות-עדיפות (הוחלט מוקדם בפרויקט) - סדר-העבודה הנוכחי (מהקשה לקל) הוא בחירת-נוחות,
לא קביעה שהפורמטים האחרים פחות חשובים.

## סכימת נתונים - הוחלט (חל על כל הפורמטים, לא ספציפי לפורמט 1969-1992)

### מבנה
- **שורה אחת = אזכור בודד באסמכתא בודדת** (לא שורה מרוכזת למקום). אם מקום שינה שם 3 פעמים,
  יהיו 3 שורות, כל אחת עם עמודת "שם קודם" שמצביעה אחורה.
- **טור עמודות אחיד לכל הפלט** (כל הטאבים/קטגוריות/דורות-פורמט) - לא סכימות שונות לכל קטגוריה.
- שתי רשימות סופיות: **רשימה א' - ישובים**, **רשימה ב' - כל השאר** (עם סיווג לפי קודי הטופונומסטיקון).

### שדות (טיוטת סכימה אחידה)
| שדה | הערות |
|---|---|
| קטגוריה | ישובים / מועצות אזוריות / אתרים היסטוריים / שמורות טבע / עצמים גיאוגרפיים / מאגרי מים / מערות / דרכים / מחלפים / צמתים / קווי דואר-נע |
| סיווג (קוד טופונומסטיקון) | 10/20/30/35/40/50/60/62/65/70/80/85/90 - ראה טבלה למטה |
| שם רשמי (עברית) | עם ניקוד כשקיים במקור |
| שם בערבית / שם בלטינית | אם קיים |
| תנועה מיישבת | **מיושם** - חולץ מהתיאור באמצעות regex, ראה נספח |
| נ"צ | **מיושם** (חילוץ) - נורמליזציה (השלמת-אפסים, ראה למטה) **טרם מיושמת בקוד** |
| סוג רשת קואורדינטות | ישן (6 ספרות) / חדש-ITM (12 ספרות) / גיליון-מפה (1953) - **טרם מיושם** |
| תיאור/מיקום חופשי | **מיושם** |
| מחוז/מועצה אזורית | כשמצוין - **טרם מיושם כטור נפרד** |
| מקור/הסבר השם | כשמצוין - **טרם מיושם כטור נפרד** |
| **שם קודם** + אסמכתא + קישור לרשומה קודמת | **טרם מיושם בכלל** - ראה "בעיות ידועות" |
| מספר גיליון / תאריך פרסום / עמוד | **מיושם** (עמוד; מספר-גיליון/תאריך קבועים לכל הרצה) |
| ציון OCR | **מיושם** - ראה למטה |

### נורמליזציית קואורדינטות - הוחלט, טרם מיושם בקוד
- הקואורדינטות תמיד מגיעות במספר זוגי של ספרות (X ו-Y).
- להשלים אפסים **מימין** עד 6 ספרות לכל קואורדינטה: `XXXYYY`/`XXX.YYY` → `XXX000/YYY000`.
- **לא** להמיר בין רשת ישנה לחדשה (ITM) - ברמת הדיוק המקורית ההמרה חסרת משמעות. לשמור סוג-רשת כשדה נפרד.

### ציון OCR - הוחלט ומיושם
- טור "ציון OCR": לכל שורה, **המינימום** מבין ציוני-הביטחון (conf) של כל המילים ברשומה (שם+גוף) -
  הכי מחמיר, חושף את המילה הכי חלשה. בנוסף: טור "ציון OCR ממוצע" ו"מילה עם הציון הנמוך".
- שני תאי-סף ניתנים-לעריכה בראש כל טאב (B1=אדום/צהוב=60, D1=צהוב/ירוק=80), עם Conditional
  Formatting מבוסס-נוסחה שמצביע עליהם (לא ערכים קבועים) - כך שעריכת-הצופה מעדכנת צבעים אוטומטית.

## טבלת קודי סיווג (מהטופונומסטיקון, לשימוש ברשימה ב')
| קוד | סוג |
|---|---|
| 10 | ישוב יהודי / מעורב ברוב יהודי |
| 20 | ישוב לא-יהודי |
| 30 | חורבה |
| 35 | אתר עתיקות, תל ארכיאולוגי |
| 40 | צומת, מחלף, גשר, מערה, מצד, מקום מיושב קטן |
| 50 | נחל, נהר, אמת-מים, מפל-מים, גיא, מעבר-מים |
| 60 | מעיין |
| 62 | באר, בור, מאגורה, גב |
| 65 | אגם, בריכה, מאגר |
| 70 | הר, רכס, גבעה, כתף, מעלה |
| 80 | שטח - עמק, רמה, מישור, חולות, מכתש |
| 85 | שמורת טבע, גן לאומי |
| 90 | ים, ימה, מפרץ, חוף, אי, שונית, נמל |
**הערה: שיוך-קוד-סיווג לכל רשומה עדיין לא מיושם בקוד** (הטבלה קיימת כהכנה לשלב הבא).

---
# ארכיטקטורת הפרסר הנוכחית (פורמט 1969-1992, גיליון 2633)

## סקירה כללית
שני קבצי-קוד משלימים: `ocr_parser.py` (פונקציות-עזר בסיסיות) ו-`ocr_parser2.py` (הפרסר
הראשי, `parse_page_v2`/`parse_full_document_v2`). רזולוציית-עבודה: **600dpi** (הוחלט: איכות
עדיפה על מהירות; 300dpi נתמך כנפילה-חזרה אך לא בשימוש בפועל).

## זרימת העבודה לכל עמוד (`parse_page_v2`)
1. **רינדור** ל-PNG ב-600dpi (`pdftoppm`).
2. **ניקוי-לכלוך** (`denoise_for_ocr`) - הסרת רכיבים-מחוברים מבודדים קטנים מסף-שטח (100 פיקסל
   ב-600dpi; 35 ב-300dpi) - מתבצע **בתוך** `ocr_tsv` אוטומטית, לפני כל OCR.
3. **OCR על העמוד המלא** (`ocr_tsv` + `ocr_letter_heights`) - גם ברמת-מילה (TSV) וגם ברמת-אות
   בודדת (`tesseract ... makebox`).
4. **זיהוי-גבול-עמודות (gutter)** - סריקת-פיקסלים ישירה (לא ניחוש-לפי-רווחי-OCR): מוצאים את
   העמודה-הכי-כהה ברצועה מרכזית של העמוד (יש קו מודפס אמיתי בין העמודות בפורמט הזה).
5. **זיהוי כותרות-סעיף** על העמוד המלא (לפני כל חיתוך) - שילוב של: גובה מעל סף (`header_min_h
   = name_threshold * 1.10`), מירכוז-לרוחב-עמוד, והתאמת-טקסט (מוצקה או מקורבת/fuzzy כנפילה-
   חזרה). שתי הגנות נגד זיהוי-שווא מתוכן-העניינים-המקדים של הגיליון (עמוד 2): (א) יותר מ-4
   כותרות בעמוד אחד → הכל נדחה; (ב) 2+ התאמות-fuzzy בעמוד אחד → רק המקורבות נדחות.
6. **פרוסות אופקיות** - חיתוך העמוד בין כותרת לכותרת (עמוד יכול להכיל כמה סעיפים).
7. **חיתוך-לעמודות** בתוך כל פרוסה (ימין/שמאל, לפי הגבול שנמצא בשלב 4).
8. **קיבוץ מילים לשורות** (`group_words_to_lines`) - לפי קרבת-מרכז-אנכי (לא top - ראה נספח
   לרציונל). `median_h` של כל שורה מחושב **מגובה-אותיות בודדות** (לא גובה-מילים), לא כולל
   מילות-רישא (מ"א/ד"נ) - זה הבסיס לכל ההבחנה שם/תיאור.
9. **סיווג שורה-לשורה** (`is_name_line`, `lines_to_entries_v2`) - שורת-שם: עד 5 מילים וגובה-
   אותיות מעל הסף המכויל; מלבד זאת - שורה מצטרפת לתיאור של הרשומה הפתוחה. מקרים מיוחדים:
   - `split_self_contained_entry` - קטגוריות כמו שמורות-טבע/מאגרי-מים שבהן כל רשומה היא שורה
     בודדת המשלבת שם+קואורדינטות, בלי תיאור נפרד.
   - `rescue_near_miss_names` - שורה קצרה שגובהה נופל ממש-מתחת-לסף אך השורה שאחריה פותחת
     בדפוס-קואורדינטות מובהק (נ"צ/ג"צ) - מוצלת כשם.
   - `NON_NAME_ADMIN_TOKEN_RE` - שורה שהיא רק "ב"+קיצור-מנהלי (במ"א/בד"נ) לעולם לא שם.
10. **המשכיות בין טור/עמוד** - "רשומה פתוחה" (`open_entry`) עוברת בין טור ימני→שמאלי באותה
    פרוסה, ובין עמוד→עמוד (כשאין כותרת חדשה) - תיאור שלא הספיק להיגמר בטור/עמוד אחד ממשיך
    ברצף לרשומה הנכונה.
11. **חילוץ שדות** מתוך גוף הרשומה: תנועה מיישבת (regex), קואורדינטות (regex).

## כיול-סף (`calibrate_document_threshold`, `calibrate_tier_threshold`)
מדגם-עמודים (בד"כ 3-4) נסרק, וכל השורות (`median_h` מבוסס-אותיות) נאספות. הסף נבחר כ**מרכז
הפער-הגדול-ביותר** בהתפלגות הממוינת (בטווח 8-70, לא כולל כותרות) - **לא** Otsu (רגיש מדי
ליחס-גדלים בין אשכולות, לא למרכז-הפער עצמו). מכויל **פעם אחת לכל גיליון**, לא לכל עמוד בנפרד.

## הרצה על כל גיליון
`run_batch_600dpi.py <תקציב-שניות>` - מריץ בסבבים (מגבלת-זמן לקריאה בודדת), שומר מצב-התקדמות
לדיסק (`scan/state_600dpi.json`: עמוד הבא, קטגוריה-נשאת, רשומה-פתוחה-נשאת, סף מכויל) עם כתיבה
**אטומית-לעמוד** (לא שורה-שורה) כדי שקריאה-שנכשלת-באמצע לא תשאיר רישומים חלקיים.
בניית קובץ Excel: `build_v3_output.py` (כולל מיון-מפורש-לפי-עמוד כהגנה, לא רק הנחה שהסדר כבר נכון).

---
# תוצאות נוכחיות - גיליון 2633 (25 עמודים)

הרצה מלאה אחרונה (600dpi, פרסר מבוסס-גובה-אותיות + זיהוי-קו-מפריד מבוסס-רכיב-מחובר,
GUTTER_STRADDLE מבוטלת, גרסה 18):
- **כל 11 הקטגוריות נמצאות**: ישובים, עצמים גיאוגרפיים, צמתים, שמורות טבע, אתרים היסטוריים,
  מועצות אזוריות, מחלפים, מאגרי מים, מערות, קווי דואר-נע, דרכים.
- **12/12 מועצות אזוריות** (מספר מאושר ע"י המשתמש מהקובץ המקורי) - כולל ארבע שהיו נעלמות/
  בעייתיות באופן חוזר בגרסאות קודמות (הערבה התיכונה, חוף עזה, עמק יזרעאל, מגילות).
- **592 רשומות סה"כ** (569 אחרי סינון רשומות ללא-קטגוריה מעמודי-הפתיחה).
- **11 אזהרות בלבד** (כולן "רצף שם-תיאור שבור") - בטאב נפרד, מיועדות לבדיקה ידנית.
- קובץ אחרון: `booklet_2633_nostraddle_20260829_224254.xlsx`.
## קבצים ומיקומים חשובים
**(עודכן למצב הנוכחי)**
- **קוד פרסר נוכחי (פעיל, גרסה 15):** `/home/claude/reshumot/ocr_parser2.py` (`parse_page_v2`,
  `parse_full_document_v2`) - מבוסס גובה-אותיות (`ocr_letter_heights`), 600dpi.
  מייבא פונקציות-עזר בסיסיות מ-`/home/claude/reshumot/ocr_parser.py` (`render_page_png`,
  `ocr_tsv`, `ocr_letter_heights`, `denoise_for_ocr`, `extract_movement`, `extract_coords`,
  `CATEGORY_KEYWORDS`) - לא כפילות, שני קבצים משלימים.
- **הרצה מלאה על גיליון (מפוצלת-סבבים, בגלל מגבלת-זמן לקריאה בודדת):**
  `/home/claude/reshumot/run_batch_600dpi.py <תקציב-שניות>` - שומר מצב-התקדמות בין סבבים
  (`scan/state_600dpi.json`), כתיבה אטומית-לעמוד.
  בניית קובץ Excel סופי מהתוצאה: `/home/claude/reshumot/build_v3_output.py`.
- **קוד ישן/היסטורי, לא בשימוש פעיל:** `/home/claude/reshumot/parse_entries_v6.py` (שכבת-טקסט
  רגילה, לפני המעבר ל-OCR).
- קלט (רשימת 42 גיליונות): `/home/claude/reshumot/scan/stage1_input_clean.json` +
  `/mnt/user-data/outputs/stage1_input_clean.xlsx`
- דוגמאות PDF שהורדו: `/home/claude/reshumot/samples/booklet_*.pdf` (277, 971, 2633, 3165, 4012, 14581)
- טופונומסטיקון (מקור להשוואה, לשלב מאוחר יותר): `/mnt/user-data/uploads/טופונומסטיקון.pdf` (232 עמ', 220MB)
  - קודי סיווג: סעיף 10.2 בהקדמה (עמ' יז בספר)
  - מבנה טור: מס' סד' / סוג / שם רשמי / שמות אחרים / תעתיק לטיני / רשת ישראל / רשת UTM / גובה / דו"ח / תאריך
  - **אזהרה:** גם קובץ זה סובל מאותה בעיית קידוד-טקסט (RTL/בידי) כמו הגיליונות הישנים - ייתכן שיזדקק לאותו
    פתרון OCR כשנגיע לשלב ההשוואה.

## הורדת PDF - הערה טכנית חשובה
יש שני hosts שמגישים את אותם מסמכים באותו path: `free-justice.openapi.gov.il` (`/free/moj/...`, כפי
שמופיע ב-URL-ים המקוריים מה-API) ו-`pub-justice.openapi.gov.il` (`/pub/moj/...`). **הזמינות שלהם
משתנה עם הזמן ובין סביבות:** בסביבת-עבודה קודמת `free-justice` נכשל בעקביות ("upstream connect
error") ורק `pub-justice` עבד; ב-9.9.2026 (סביבה חדשה) המצב היה הפוך - `pub-justice` החזיר
HTTP 500 (`{"code":500,"message":"Internal Server Error"}`, גוף-JSON קצר במקום PDF) ו-`free-justice`
עבד. **כלל:** לנסות את שני ה-hosts, ולאמת שהתוצאה היא PDF אמיתי (`pdfinfo`/מספר-עמודים תואם
לעמודה "סה"כ עמודים בגיליון") - לא להסתמך על קוד-HTTP בלבד.

### התקנת Hebrew tesseract (נדרש בכל סביבת עבודה חדשה)
```bash
curl -sL -o /tmp/heb.traineddata "https://raw.githubusercontent.com/tesseract-ocr/tessdata/main/heb.traineddata"
cp /tmp/heb.traineddata /usr/share/tesseract-ocr/5/tessdata/heb.traineddata
```
**חובה לאמת גודל-קובץ אחרי ההורדה (~5.4MB).** ב-9.9.2026 הכתובת `github.com/.../raw/main/...`
החזירה **גוף-שגיאה של 378 בתים** (JSON של חסימת-גישה) עם קוד-הצלחה, והקובץ ה"מותקן" גרם ל-
`Error opening data file ... Failed loading language 'heb'` - כלומר **0 מילים בכל OCR, בלי
הודעת-שגיאה בצינור עצמו**. להשתמש ב-`raw.githubusercontent.com` ולבדוק `ls -la`.
**אזהרה על חלופת-apt:** `apt-get install tesseract-ocr-heb` מתקין מודל **ישן** (961KB, גרסת
2019/legacy) - הוא עובד, אבל אינו אותו מודל שכל ערכי-הסף במסמך הזה כוילו מולו. לא להסתפק בו.

---
# בעיות ידועות, נושאים פתוחים לדיון ולפיתוח

## ממתין לאישור מפורש מהמשתמש - אין להתחיל בלי אישור
- **הערכה-מחדש של מנגנון ה-fuzzy** (`fuzzy_detect_header_category`) - המשתמש ביקש להוסיף
  את זה כמשימה לסוף העבודה, ואמר במפורש: "אין להתחיל במשימה זו ללא אישור שלי".
  - **הבעיה שהמנגנון פותר:** כותרת-סעיף אמיתית (למשל "10. שמות למחלפים") נסרקת לפעמים ע"י
    ה-OCR בצורה מעוותת כל-כך שאפילו מילת-המפתח הנדרשת ("שמות", "מחלפים") לא מופיעה כתת-
    מחרוזת מדויקת בתוצאה (למשל נקראת "0. עמות למחלפים"). זיהוי-מוצק (substring רגיל) נכשל
    במקרים כאלה **לגמרי** - וללא-fuzzy, כל תוכן הסעיף הזה (לא רשומה בודדת - **כל הסעיף**)
    היה נספח בשקט לקטגוריה הקודמת (אותה תופעת "דליפת-קטגוריה" שכבר ראינו כמה פעמים).
  - **חומרת הבעיה כרגע (נבדק בפועל, לא הערכה):** בהרצה הנוכחית, **2 מתוך 11 הקטגוריות**
    ("מחלפים", "קווי דואר-נע") **תלויות לחלוטין ב-fuzzy** לזיהוי הכותרת שלהן - הזיהוי-המוצק
    נכשל על שתיהן (`strict=None`), ורק ה-fuzzy תופס אותן. כלומר: המנגנון **חיוני כרגע**, לא
    רשת-ביטחון-שולית - ביטולו-בלי-תחליף יחזיר לפחות 2 קטגוריות למצב-נעלם.
  - המנגנון עצמו פעיל בקוד (סף דמיון 0.65) ועובד, אבל לא נבדק-מחדש בעומק כפי שהוסכם.
  - **היקף-הבעיה רחב יותר ממה שנראה (הבהרה חשובה, טרם נבדק):** עיוות-OCR-עד-כדי-כישלון-
    התאמת-טקסט הוא תופעה **כללית שראינו בכל חלקי המסמך** (שמות: "משה אשר" במקום "מטה אשר";
    תיאורים: "רמרדפים"/"ניצנר" במקום "המרדפים"/"ניצנה"), **לא ייחודית לכותרות**. ה-fuzzy
    הנוכחי מיישם פתרון **רק בנקודה אחת** (זיהוי-כותרות) - יש עוד כמה מקומות בצינור שמסתמכים
    על התאמת-טקסט-פחות-או-יותר-מדויקת בלי שום הגנה מקבילה, ופגיעים לאותה תקלה עקרונית:
    `DESCRIPTION_OPENER_RE` (זיהוי נ"צ/ג"צ בתחילת-תיאור, ב-`rescue_near_miss_names`),
    `SUBHEADER_MAP_RE` (סינון "מפה מס'..."), `NON_NAME_ADMIN_TOKEN_RE` (חסימת "במ"א"/"בד"נ"),
    וחילוץ תנועה/קואורדינטות (regex). המשמעות: המשימה-הממתינה-לאישור עשויה להיות רחבה יותר
    ממה שנראה במבט ראשון - לא רק "האם 0.65 הסף הנכון לכותרות", אלא כנראה "האם דרושה גישה
    עמידה-ל-OCR כללית יותר לכל הצינור, לא רק לזיהוי-כותרות". **לא נבדק, לא הוחלט.**

## אזהרות/מדדים לא-נקיים שטרם הוכרעו
- **GUTTER_STRADDLE - בוטלה (גרסה 18, החלטת משתמש).** נבדקו כל האזהרות; אפס תפיסות-אמיתיות
  (כותרות/רעש-שוליים/ארטיפקט-"|" חוזר בלבד). הקוד עצמו (`words_straddling_x`) נשאר מוגדר,
  לא נקרא. אם גיליון/פורמט עתידי יגלה חיתוך-מילה-אמיתי, לחזור על הבלוק המוער ב-`parse_page_v2`.
- **11 האזהרות הנוכחיות** (כולן מהסוג "רצף שם-תיאור שבור") לא עברו סקירה-ידנית שיטתית מלאה.

## שדות/יכולות מהסכימה המוסכמת שטרם מיושמים
1. **"שם קודם" + אסמכתא + קישור לרשומה קודמת** - לא מיושם בכלל. יש דוגמת-טקסט שנצפתה
   ("השם הקודם 'מעלה הגליל' - בטל") אך לא נבנה חילוץ שיטתי.
2. **נורמליזציית קואורדינטות** (השלמת-אפסים ל-6 ספרות) - הוחלט אך לא קודד.
3. **סוג-רשת-קואורדינטות** (ישן/ITM/גיליון-מפה) - לא מיושם כטור נפרד.
4. **מחוז/מועצה-אזורית ומקור/הסבר-השם** כטורים נפרדים - לא מיושמים (המידע קיים בתוך "תיאור
   חופשי" אך לא חולץ לטור ייעודי).
5. **שיוך קוד-סיווג טופונומסטיקוני** (10/20/30...) לכל רשומה - הטבלה קיימת, השיוך בפועל לא.
6. **שמות בערבית/לטינית** - לא חולצו כטורים נפרדים גם כשמופיעים בתיאור.

## גרסה 19 (בעבודה) - שיפור זיהוי שורת-שם/תיאור: שילוב רווח+רצף+ניקוי-מוקדם

### רקע - סדרת בדיקות עם המשתמש על עמוד 13
בעקבות בעיות חוזרות בזיהוי שורות-שם (ראו "האם ואיך לתמוך בניקוד" למעלה), נערכה סדרת-בדיקות
מפורטת על העמודה הימנית של עמוד 13 (11 רשומות, 28 שורות-תוכן):
1. **גילוי:** שורת-שם/תיאור נבדלות בברור **גם** לפי רווח-אנכי (לא רק גובה): מעבר-בין-רשומות
   60-230px, שם→תיאור-פותח 45-64px, המשך-פסקה 17-36px.
2. **מיזוג-חפיפה-ב-Y:** מילים/סימנים שחופפים ב-Y (או שהרווח ביניהם קטן מחצי-המינימום-בין-
   שורות-תיאור) ממוזגים לשורה אחת לפני סיווג - פותר ניקוד/רעש-זעיר שנקלט כ"שורה" נפרדת.
3. **מבנה-רצף עם איפוס-אחרי-מפה:** תוכן קטגוריה = חזרות של [שורת-מפה אופציונלית]→שם→תיאור
   (אחד+). מכונת-מצבים חד-מעברית: FORCE_NAME (התחלת-עמודה/אחרי-מפה) → FORCE_BODY (אחרי-שם,
   בלי בדיקה) → CHECK (אחרי-תיאור: רווח>40 משני-צדדים מחליט שם-חדש או המשך-פסקה). שורת-מפה
   **תמיד** מאפסת ל-FORCE_NAME, גם אם הגיעה תוך-כדי-מצב-שגוי (מונע שרשור-שגיאות).
4. **ניקוי-מוקדם (3 שינויים, כן שולבו בקוד בפועל - ראו למטה):** גובה-מינימלי + בידוד-אופקי
   לסינון-רעש, והסרת-שוליים-תחתונים-ריקים לפני כל עיבוד.

תוצאה מצטברת על עמוד-הבדיקה: מ-9 שגיאות (32%) ל-2 (7%) מתוך 28 שורות-תוכן.

### מה שולב בפועל בקוד הראשי (`ocr_parser.py`/`ocr_parser2.py`)
- **הסרת-שוליים-תחתונים** (`render_page_png`) - נמדד עקבי (5.7%-6.0%) על 3 עמודים; קוצץ 5%
  קבועים מיד אחרי הרינדור, לפני כל עיבוד אחר.
- **`filter_isolated_noise_words`** - סינון מילים לפי גובה-מינימלי (25px) **וגם** בידוד-אופקי
  (מרווח לשכן-הקרוב-ביותר-בטווח-Y-חופף מעל 40% מרוחב-העמודה) - מופעל על כל טור מיד אחרי OCR,
  לפני קיבוץ-לשורות. נבדק: רעש אמיתי (18-19px, מבודד 82%-87% מרוחב-עמודה) מול תוכן אמיתי
  (38-93px, מרווח 13%-23%) - הפרדה נקייה.

### מה **עדיין לא** שולב (נבדק רק כסקריפט-אב-טיפוס עצמאי, לא בצינור האמיתי)
- מיזוג-חפיפה-ב-Y (שלב 2 למעלה) - עדיין לא ב-`group_words_to_lines`.
- מכונת-המצבים מבוססת-רווח+רצף (שלב 3) - עדיין לא ב-`is_name_line`/`lines_to_entries_v2`
  (אלה עדיין משתמשים בבדיקת-גובה-בלבד הקיימת).
- **בדיקה על עמוד 13 אחרי שילוב 3 השינויים-שכן-בקוד בלבד** (בלי מיזוג/מכונת-מצבים) מראה
  שיפור חלקי: "0" נעלם כמצופה (סינון-רעש), והכותרת-התחתית **גם היא** נעלמה - אך **תיקון
  מאוחר יותר גילה שזה לא היה בזכות חיתוך-השוליים**: הכותרת-התחתית נעלמת תמיד בזכות
  `PAGE_FOOTER_RE` (סינון-טקסט קיים-מראש, לא קשור לחיתוך-הפיקסלים) - ראו "בירור נפרד:
  מה חיתוך-השוליים באמת עושה" למטה. "נערן" עדיין חסרה ו"מפות מס' 12-11..." עדיין מופיעה
  כרשומה-שגויה - **כי אלה תלויים בשני השינויים שעדיין לא שולבו**.

### בירור נפרד (מאוחר יותר): מה חיתוך-השוליים-התחתונים באמת עושה
נבדק ישירות על כל 25 העמודים (לא רק 3), בעקבות שאלת-בירור של המשתמש: **הכותרת-התחתית
("ילקוט הפרסומים...") שורדת את חיתוך-הפיקסלים ב-21 מתוך 25 עמודים** - היא ממוקמת
נוחות בתוך התחום-שנשמר (מרווח 86-92px מהתחתית, לא קרוב לגבול-החיתוך). **המנגנון שבאמת
מסיר אותה מהרשומות הוא `PAGE_FOOTER_RE`** (סינון-טקסט, שורות 30+440, קיים עוד לפני
שינוי-חיתוך-השוליים) - **לא** קשור לחיתוך-הפיקסלים בכלל. חיתוך-הפיקסלים (5%, מאומת
עקבי 0.9497-0.9500 בכל 25 העמודים, בסדר-הנכון לפני-חיתוך-עמודות בשני נתיבי-הקוד הפעילים)
מוריד **רק** שטח-ריק-אמיתי מתחת לתוכן-האחרון, לא את התוכן-האחרון-עצמו. תיעוד קודם שטען
שהכותרת-התחתית נעלמת "בזכות" חיתוך-השוליים היה שגוי - תוקן כאן.

**המשך נדרש:** לשלב את שלב 2 (מיזוג) ושלב 3 (מכונת-מצבים) בפועל בקוד, ואז להריץ-מחדש-הכל
ולאמת על כל הגיליון, לא רק עמוד 13.

## מגבלות ידועות של גישת-גובה-האותיות (גרסה 15)
- **האם ואיך לתמוך בניקוד** - נושא פתוח חדש (לא להתחיל בלי דיון נוסף): ניקוד תמיד מנותק-
  פיזית מהאות שמעליו (תכונה טיפוגרפית בסיסית, לא תלוית-ניקוי-שלנו - אומת ישירות על "נערן",
  עמ' 13: הניתוק קיים כבר בתמונה הגולמית, לפני כל עיבוד). לפעמים Tesseract משייך נכון ניקוד
  למילה שמעליו, לפעמים לא (לא ידוע למה ההבדל, לא נבדק לעומק - שאלה על ההיגיון-הפנימי של
  Tesseract עצמו). **חשוב:** בהמשך נצטרך לבחון את איכות-זיהוי-הניקוד של ה-OCR - **אין למחוק
  מידע-ניקוד** כפתרון-ביניים, גם אם זה נראה כמו "רעש" מבחינת סיווג-שורות.
- **נבדק על מדגם קטן**: 3-4 עמודים מגיליון 2633 + עמוד בודד מגיליון **שונה** (3165). לא נבדק
  על פני הגיליון המלא (25 עמ') ברמת-דיוק-אות, רק ברמת-תוצאה-סופית (ספירת-רשומות/קטגוריות).
- **לא נבדק על שאר 41 הגיליונות** - כולל 3 הפורמטים האחרים שטרם התחלנו, וגיליונות נוספים
  מאותו פורמט (1969-1992) שעשויים להיסרק באיכות/רזולוציה שונה.
- **שיוך אות→מילה** (לפי חפיפת-מיקום בין פלט ה-TSV ל-makebox) - לא נבדקו מקרי-קצה שיטתית
  (מילים חופפות, רווחים חריגים).
- **ניקוד כבד מנפח גם אותיות "רגילות"** - נצפה שמילים עם 2-3 סימני-ניקוד (כמו "גולן",
  "גוש עציון", "דרום השרון") מקבלות ערכי-גובה-אות חריגים (75-99) גם באותיות שאינן ל'/סופיות.
  לא נבדק אם זה עלול לפגוע בהפרדה שם/תיאור בטקסט מנוקד-במיוחד. **בבדיקה שכן בוצעה**, זה לא
  קרה בפועל בתיאורים (רק אולי בבדיקה בודדת מוטה, ראה נספח) - אך לא הוכרז כפתור-לגמרי.
- **עלות-ביצועים**: `makebox` הוא מעבר-OCR שלישי (בנוסף לעמוד-מלא ולכל-עמודה-חתוכה). זמן
  עיבוד כולל לעמוד (רינדור+ניקוי+3 מעברי-OCR+ניתוח) נמדד ~20-40 שניות; לא נבדק אופטימיזציה.

## שגיאות-תוכן ספציפיות שנצפו ולא טופלו
- **רעש-פנימי בתוך שם** - "מ"א הערבה התיכונה" נקלטת לעיתים עם תוספת-רעש ("- 0") שמעוותת את
  הטקסט גם כשהגובה/הסיווג נכונים. חציון-גובה-אותיות פותר רק את בעיית-הגובה, לא את כל מקורות-
  השגיאה בתוכן.
- **וריאציה בין-הרצות (run-to-run)** - אותו עמוד, מרונדר-מחדש, יכול לתת מדידות-גובה שונות
  במקצת ולפעמים לשנות אם כותרת/רשומה מזוהה נכון. זו תופעה חוזרת (נצפתה גם בגרסאות קודמות)
  ולא נמצא לה פתרון מלא - רק הפחתה הדרגתית של הרגישות אליה.
- **קטגוריות עם ספירה נמוכה** ("דרכים"=2-3, "קווי דואר-נע"=3-5 בהרצות שונות) - לא הוכרע אם
  אלה המספרים האמיתיים בגיליון (קטגוריות קטנות מטבען) או עוד תת-זיהוי שטרם אובחן.

## שלבים גדולים שטרם התחילו
- **3 פורמטים נוספים** (1953-58, 1959-68, 2006-26) - אפס עבודה עד כה.
- **41 גיליונות נוספים** מתוך 42 - נבדק (חלקית) רק גיליון 2633, ובמידה מוגבלת גיליון 3165.
- **שלב ההשוואה לטופונומסטיקון** - לא התחיל; ידוע כבר שהקובץ סובל מאותה בעיית-קידוד (RTL/בידי)
  כמו הגיליונות הישנים ועשוי לדרוש אותו פתרון-OCR.

---
# נספח א' - יומן פיתוח כרונולוגי

מסמך זה הוא היומן המלא, גרסה-אחר-גרסה, של תהליך הפיתוח - אבחונים, ניסיונות שנכשלו ונדחו,
דיוני-בדיקה מפורטים עם המשתמש, וההיגיון מאחורי כל החלטה. **המספרים (ספירות-רשומות, אזהרות
וכו') בסעיפים המוקדמים כאן היסטוריים בלבד** - הם שוקעו ע"י הרצות מאוחרות יותר. למצב הנוכחי,
ראו את גוף המסמך למעלה.

## פורמט 1969-1992 (בעבודה)

### בעיה טכנית מרכזית: 2 עמודות
**(היסטורי - שיטה שהוחלפה, ראה עדכון)** pdftotext (רגיל או -layout) מערבב בין העמודות.
ניסיון ראשון: חיתוך לפי קואורדינטות (pdfplumber), למצוא את ה"מרווח" (gutter) הגדול ביותר
בהתפלגות ה-x0 של המילים בעמוד.
**עדכון (גרסה 6 ואילך):** שיטה זו הוחלפה **בזיהוי-קו-מפריד ישיר מפיקסלי-התמונה** (`find_gutter_x`,
`ocr_parser2.py`) - נמצא שיש קו מודפס אמיתי בין העמודות בגיליונות אלה, וזיהוי-פיקסלים ישיר
(עמודת-הפיקסלים הכי-כהה ברצועה מרכזית) הרבה יותר יציב ומדויק מניחוש-לפי-רווחי-OCR. ראה "גרסה 6"
למטה לפרטים.

### בעיה טכנית שניה: שכבת הטקסט המוטבעת פגומה עבור טקסט עם ניקוד
דוגמאות תיעוד:
- "מ״א מטה אשר" → יצא "מ*א מטהאטר" משכבת הטקסט הרגילה (שגיאת זיהוי אותיות, לא רק היפוך RTL)
- "מ״א מטה בנימין" → יצא "מ*א מטה בממין"

**פתרון (הוחלט ומיושם חלקית):** לוותר על שכבת הטקסט המוטבעת, ולהשתמש ב-**OCR אמיתי**:
1. `pdftoppm` render בעמוד ברזולוציה גבוהה (300 dpi)
2. חיתוך לתמונה ימנית/שמאלית (`PIL`)
3. `tesseract -l heb --psm 4 tsv` על כל חצי בנפרד (לא על העמוד השלם - נותן תוצאה הרבה יותר נקייה)
4. משתמשים בפלט ה-TSV (כולל קואורדינטות ברמת מילה + ציון ביטחון (conf)) לבנייה מחדש
   של זיהוי גבולות-רשומה (כמו קודם) + לקבל טקסט נקי עם ניקוד.
- `heb.traineddata` הורד והותקן ידנית (`/usr/share/tesseract-ocr/5/tessdata/heb.traineddata`) -
  לא מותקן כברירת מחדל בסביבה, צריך להוריד מ-`https://github.com/tesseract-ocr/tessdata/raw/main/heb.traineddata`
  בכל סביבת עבודה חדשה.

### ארכיטקטורת ה-OCR - גרסה ראשונה (`ocr_parser.py`, `slice_and_parse_page`)
**(היסטורי - הוחלפה ב`ocr_parser2.py`/`parse_page_v2`, ראה "גרסה 2" ואילך למטה. הכותרת
"הסופית" למטה הייתה נכונה רק בזמנה.)**

**זרימת העבודה לכל עמוד** (`slice_and_parse_page`):
1. רינדור לתמונה ב-300dpi (`pdftoppm`).
2. OCR על **העמוד המלא** (לא חתוך) - `tesseract -l heb --psm 4 tsv` - כדי לזהות את כותרות הסעיף
   (הן חוצות את כל רוחב העמוד, וחיתוך-עמודות מוקדם מדי היה קוטע אותן).
3. **פרוסות אופקיות**: העמוד נחתך ל"פרוסות" בין כותרת לכותרת (עמוד יחיד יכול להכיל כמה כותרות-סעיף,
   למשל "3. קווי דואר נע" ואחריו "4. שמות אתרים היסטוריים" - כל אחת פותחת פרוסה חדשה שמשתרעת על
   פני שתי העמודות). **לא** מבצעים חיתוך-עמודות אחיד לעמוד השלם.
4. בתוך כל פרוסה: OCR נפרד על חצי ימני וחצי שמאלי (חיתוך ב-50% רוחב).
5. כל הרשומות שנמצאות בפרוסה מקבלות ישירות את קטגוריית-הפרוסה (`e["category"] = s_cat`) - **לא**
   ניחוש-קטגוריה מקומי לפי כותרות בתוך כל טור (זה היה מקור הבאגים - ראה "באגים שנפתרו" למטה).

### באגים שהתגלו ונפתרו (סדר גילוי, כולם ב-`ocr_parser.py`)
1. **דליפת שורות TSV גולמיות לתוך הטקסט** - Tesseract מזהה גרש עברי (`"`) כחלק מטקסט (כמו
   ב-`"מפעלות אפק"`), ומודול ה-CSV של פייתון מפרש את זה כתו-ציטוט CSV פותח ו"בולע" שורות שלמות עד
   שהוא מוצא גרש סוגר. **תיקון:** `csv.reader(..., quoting=csv.QUOTE_NONE)`.
2. **כותרות חוצות-עמודות + כותרות מרובות באותו עמוד** - ראה "ארכיטקטורה" למעלה. גילינו (בעזרת
   תמונת עמוד אמיתית מהמשתמש) שעמוד יחיד יכול להכיל כמה כותרות-סעיף ברצף, וחיתוך-עמודות חייב לקרות
   *בין* הכותרות, לא לעמוד השלם.
3. **`gap` שלילי (סדר-שורות "לא מונוטוני" למראית עין)** - מילה בודדת עם ניקוד כבד (כמו "אָשַר") מקבלת
   תיבת-OCR גבוהה בצורה חריגה (80px במקום ~30px הרגיל), מה שמנפח את ה"תחתית" המדומה שלה וגורם לחישוב
   gap שגוי (שלילי) לשורה הבאה - למרות שה-`top` (תחילת כל שורה) עולה בצורה מונוטונית לגמרי.
   **תיקון:** לחשב `gap` לפי top-to-top (לא top-to-bottom-של-הקודם) - עמיד בפני חריגות-גובה כאלה.
4. **קיבוץ מילים לשורות לפי `top` נכשל בגלל 3 גדלי גופן** - בפורמט הזה יש כותרות / שמות / טקסט-רגיל
   בגדלים שונים, ובתוך שורת-שם יש **רישא קטנה** (מ"א, ד"נ, תל, מצודת...) **ממורכזת אנכית** ביחס לשם
   שאחריה (גודל בינוני) - לא מיושרת לפי הבסיס כמו טקסט רגיל. רישא ("מ"א", top=235, h=21) והשם
   שאחריה ("אשדות", top=225, h=46) נבדלים ב-`top` ב-~10-14px (מספיק כדי להיחשב בטעות לשתי שורות
   נפרדות), אבל ה**מרכז** האנכי שלהם (top + height/2) כמעט זהה (הפרש 1-6px בלבד).
   **תיקון:** לקבץ מילים לשורות לפי קרבת-מרכז (`top + height/2`), לא לפי קרבת-`top`.
5. **זיהוי-כותרת-שווא בתוך טקסט רגיל** - מילת המפתח "יישובים" (לזיהוי כותרת "שמות ליישובים") היא
   גם תת-מחרוזת של "היישובים", שמופיעה עשרות פעמים כחלק מטקסט רגיל ("...בתוכה את היישובים: X, Y...").
   `detect_category()` הפשוט (בדיקת substring) תפס false positives על כל תיאור-מועצה.
   **תיקון:** `detect_header_category()` מחמיר - כותרת אמיתית חייבת להכיל גם "שמות ל" (או להיות כותרת
   "מועצות אזוריות" עם מספר-סעיף בהתחלה). `detect_category()` הרגיל (הרפוי) עדיין קיים ומשמש למקומות
   אחרים שבהם ה-substring הפשוט מספיק בטוח.

**תוצאה על עמוד הבדיקה (עמ' 3, גיליון 2633):** 24 רשומות, כל 13 רשומות המועצות מתויגות נכון
"מועצות אזוריות" וכל 11 רשומות הישובים מתויגות נכון "ישובים" - התאמה מלאה למבנה האמיתי של העמוד.
עדיין נשארות שגיאות-תווים נקודתיות (כמו "מ"א משה אשר" במקום "מ"א מטה אשר") - בדיוק המקרים שציון
ה-OCR (min confidence, ראה סעיף "ציון OCR" למעלה) אמור לסמן לבדיקה ידנית.

### חילוץ שדות מתוך גוף הרשומה (body)
- **תנועה מיישבת**: regex `(?:משקי\s+)?תנועת\s+(.+?)(?:,|\s+ב[א-ת]{2,}|\s*\(|\.)"` - עובד טוב על רוב המקרים.
  דוגמאות תנועות שראינו בפועל: המושבים, המושבים הדתיים/הפועל המזרחי, העובד הציוני, הקיבוץ המאוחד,
  הקיבוץ הארצי, האיחוד החקלאי, החרות-ביתר, איחוד הקיבוצים והקבוצות, הקיבוץ הדתי, פועלי אגודת ישראל,
  התאחדות האכרים.
- **קואורדינטות**: regex `[נג]["*׳״]?צ\s*[,:]?\s*([\d.]+)` (גם נ"צ וגם ג"צ - כנראה תקלדה/OCR של אותה
  משמעות, "ג" ליד "נ" במקלדת).
- **שינויי שם**: **טרם מומש** - להכניס בבנייה הבאה. תבנית טקסט לדוגמה שראינו: "השם הקודם 'מעלה הגליל' - בטל"
  (למועצה אזורית, לא רק ליישוב - מאשר שהשדה חל על כל הקטגוריות).

### דוגמת קובץ שנבנה (לבדיקה, לא סופי - **מהגרסה הישנה, טרם עודכן ל-OCR**)
`booklet_2633_parsed_sample.xlsx` - 325 רשומות מתוך גיליון 2633 (25 עמ'), עדיין עם שכבת הטקסט
הרגילה (לפני מעבר ל-OCR). **יש להריץ מחדש עם `slice_and_parse_page` על כל 25 העמודים ולעדכן.**

## גרסה 2 - פרסר מבוסס-פסקאות (לפי בקשת המשתמש, בעקבות באג עמוד-2)

### הבעיה שהובילה לבנייה מחדש
זוהה שעמוד 2 של גיליון 2633 (עמוד הפתיחה/תוכן-עניינים/ביאורים/חתימה של הגיליון עצמו - **לא**
נתונים) נקלט בטעות כ-28 "רשומות מועצה" מדומות. הסיבה: הזיהוי הישן הסתמך על טקסט (regex) בלבד,
וטקסט תוכן-העניינים המקדים ("1. למועצות אזוריות; 2. ליישובים...") הכיל את אותן מילות-מפתח כמו
כותרת-סעיף אמיתית, בלי שום בדיקת-מבנה חזותי שתבדיל ביניהם.

### הארכיטקטורה החדשה (`ocr_parser2.py`) - לפי גודל-אות אמיתי, לא regex
1. **חציון-גובה-שורה** (`median_h`, לא max/top) מפריד בצורה נקייה בין 4 "שכבות": רעש (2-4px),
   תיאור (20-28px), שם (34-50px), כותרת (45-66px, עם חפיפה מסוימת מול שם). שימוש בחציון (לא מקסימום)
   כי מילה בודדת עם ניקוד כבד יכולה לנפח את הגובה המקסימלי (ראה גרסה 1, "אָשַר"=80px) בלי להשפיע
   על החציון של כל השורה.
2. **קליברציה ברמת-הגיליון, לא ברמת-העמוד** - ניסיון ראשון לכייל סף לכל עמוד בנפרד נכשל בדיוק
   בעמודים בלי תוכן-שם אמיתי (כמו עמוד 2) - האלגוריתם "המציא" חלוקה משמעותה כי הוא חייב למצוא
   *איזשהו* פער. הפתרון: לכייל **פעם אחת לכל הגיליון**, מדגימת עמודים (בד"כ 3-7, מדלגים על 1-2
   שסביר שהם כריכה/תוכ"ע), ולהשתמש באותו סף לאורך כל העמודים - הגיוני יותר גם כי גודל הגופן עקבי
   באותו ספר.
3. **שיטת כיול: Otsu** (לא "הפער הגדול ביותר בהתפלגות") - ניסיון עם "biggest gap" לא היה יציב:
   על עמוד בודד נתן תוצאה טובה (34.8), אבל בצירוף כמה עמודים מצא לפעמים את הפער הלא-נכון (בין
   שם לכותרת, במקום בין תיאור לשם), וקפץ ל-53.8 (יותר מדי מחמיר, כמעט הכל הפך ל"תיאור").
   Otsu (אלגוריתם קלאסי לבינריזציה של תמונות, `cv2.threshold(..., THRESH_OTSU)`, מיושם כאן על
   התפלגות הגבהים במקום עוצמות-פיקסלים) יציב הרבה יותר: 31.0 (עמוד בודד) לעומת 32.0 (5 עמודים).
4. **סף נפרד וגבוה יותר לזיהוי כותרות** (`header_min_h = name_threshold * 1.15`) - נדרש כי
   טקסט תוכן-העניינים המקדים בעמוד 2 מתברר כמודפס בגודל-ביניים (גדול מתיאור, אבל לא קטן בהרבה
   מ"שם"), כך שסף השם הרגיל (32) לא הספיק לדחות אותו. מכפיל של 1.3 היה אגרסיבי מדי (פסל גם
   כותרות אמיתיות: אתרים היסטוריים/שמורות טבע/עצמים גיאוגרפיים/מאגרי מים נעלמו). 1.15 איזן טוב:
   כל 9 הכותרות שנמצאו בגרסה 1 עדיין נמצאות, ועמוד 2 לא מייצר כותרת-שווא.
5. **אימות רצף שם-תיאור-שם-תיאור** - כל פעם ששתי שורות "name-tier" מופיעות ברצף בלי תיאור
   ביניהן, נרשמת אזהרה (`warnings`) במקום ניחוש שקט. שימושי לאיתור קטגוריות עם/בלי-תיאור (ראה
   סעיף הבא) ולבדיקה ידנית.
6. **התעלמות מכותרות-משנה "מפה מס'..."** - `SUBHEADER_MAP_RE` מסנן שורות מהצורה
   "מפ(ה/ות) מס' X (קנ"מ 1:100,000):" מכל הקטגוריות, כפי שהתבקש.

### תוצאה על גיליון 2633 המלא (25 עמ')
**447 רשומות אמיתיות** (אחרי סינון 23 רשומות-רעש מעמוד 2 שעדיין מופקות שם אך מסומנות
`category=None` וללא כותרת אמיתית - ולכן מסוננות בבנייה הסופית, לא "נעלמות בשקט").
כל 9 הקטגוריות הגדולות זוהו נכון וברצף הנכון: מועצות אזוריות (12) → ישובים (163) → אתרים
היסטוריים (18) → שמורות טבע (71) → עצמים גיאוגרפיים (103) → מאגרי מים (5) → מערות (6) →
דרכים (14) → צמתים (55). **עדיין חסרות**: קווי דואר-נע ומחלפים (קטגוריות זעירות, ~4 רשומות
כל אחת בגרסה 1 - טרם אומת בגרסה 2).
346 רשומות עם תיאור, 109 בלי - **טרם אומת** אילו מהן חוסר-תיאור אמיתי (כמו "מאגרי מים", לפי
המשתמש) לעומת פער-זיהוי אמיתי שדורש תיקון.

### מה עדיין לא מומש מהתכנון (`REBUILD_PLAN.md`)
- **מבנה מיוחד ל"שמורות טבע"** (2 טורים לכל שם - מיקום ושם - בלי הרישא "שמורת" בכל רשומה,
  עם "שמורת" מופיעה פעם אחת בראש כל טור) - כרגע מטופל כמו כל קטגוריה רגילה (שם+תיאור), כנראה
  לא נכון. 71 הרשומות בקטגוריה זו צריכות בדיקה/תיקון ייעודי.
- אימות שיטתי, קטגוריה-קטגוריה, איזה מבנה חל עליה (עם-תיאור-רגיל / בלי-תיאור-כלל / מבנה-מיוחד).
- קובץ הפלט: `booklet_2633_v2.xlsx` (כולל טור "יש תיאור?" חדש, וכל טורי ה-OCR מגרסה קודמת).

### קבצים
- `ocr_parser2.py` - הפרסר החדש (מבוסס-פסקאות/גודל-אות). `ocr_parser.py` הישן עדיין קיים
  ומכיל את פונקציות העזר הבסיסיות (`render_page_png`, `ocr_tsv`, `extract_movement`,
  `extract_coords`) ש-`ocr_parser2.py` מייבא ממנו - לא כפילות, המשך-שימוש מכוון.

## גרסה 2.1 - תיקוני באגים בעקבות בדיקת המשתמש (עמוד 2 + "מטה אשר" + "עלי-על")

### באג 1: כותרות עדיין דלפו לרשימת הרשומות (למרות התיעוד ש"נפתר")
**הסיבה שהתגלתה:** הפרוסה (slice) בין כותרת לכותרת הוגדרה מ-`top` של הכותרת (לא אחרי הכותרת),
כך שכשחותכים לעמודות ימין/שמאל, שורת הכותרת עצמה (שרוחבה חוצה את שתי העמודות) נחצית לשני
חצאים - וכל חצי נקרא שוב בתוך ה-OCR-לפי-עמודה כאילו היה רשומה משלו ("1. שמות למ" / "ועצות
אזוריות..."). **תיקון:** לחשב גם `top` וגם `bottom` לכל כותרת, ולהגדיר פרוסה = מ-`bottom` של
הכותרת הקודמת (+שוליים קטנים) עד `top` של הכותרת הבאה - כך ששורת הכותרת עצמה **אף פעם** לא
נכללת בתוך אף פרוסה. תיקון ראשון שגוי (השתמש ב"תחילת הפרוסה הבאה" כגבול-סיום, שכבר כולל מרווח
*אחרי* הכותרת הבאה) גרם לפרוסה הקודמת "לבלוע" גם את שורת הכותרת הבאה - תוקן בסבב שני.

### באג 2: שלב-הפסקאות לא מומש בפועל (רק סווגו שורות בנפרד, לא פסקאות)
**הסיבה:** גרסה 2 הראשונה קיבצה מילים לשורות (`group_words_to_lines`) וסיווגה **כל שורה
בנפרד** לפי `median_h` שלה - בלי שלב-ביניים של איגוד-לפסקאות. זה בניגוד מפורש לתכנון
(`REBUILD_PLAN.md`, שלב 1) וגרם לשתי תופעות:
- **"עלי-על":** מילה בודדת בתוך פסקת-תיאור רב-שורתית שקיבלה (מרעש-OCR) גובה גדול מהרגיל
  נחשבה בטעות ל"שורת שם" חדשה, ופיצלה את פסקת-התיאור לשתיים.
- **"מטה אשר":** תיאור בן-שורה-אחת (או שנקרא ככזה) קיבל גובה חציוני מעל הסף, ונחשב בטעות
  לשם נפרד במקום להיצמד לשם שלפניו.

**תיקון (`group_lines_to_paragraphs` + `is_name_paragraph`):** קודם מאגדים שורות לפסקאות
לפי מרווח אנכי (`gap`, כמו קודם - top-to-top, עמיד לחריגות ניקוד). **רק אז** מסווגים כל
**פסקה** (לא שורה): פסקת-שם = פסקה בת-שורה-אחת **בלבד**, שגם `median_h` שלה מעל הסף. פסקה
מרובת-שורות היא **תמיד** פסקת-תיאור, ללא קשר לגובה מילה בודדת בתוכה - זה בדיוק מה שפותר את
מקרה "עלי-על" (מילה חריגה בתוך פסקה רב-שורתית כבר לא יכולה לפצל אותה, כי הבדיקה חלה ברמת
הפסקה השלמה, לא ברמת השורה הבודדת).

**מגבלה שנותרה (לא "באג" הניתן לתיקון מלא, אלא רעש-מדידה מובנה):** גם ברמת פסקה-בת-שורה-אחת,
`median_h` עדיין מבוסס על מדידת-OCR שיש בה שונות בין הרצות. נצפו מקרים גבוליים: "מ\"א הערבה
התיכונה" קיבל 30.0 (מתחת לסף 32 - נבלע בטעות לתיאור הקודם), ו"מ\"א חוף עזה" קיבל 26.0 (אותה
בעיה). ולהפך: "מ\"א משה אשר" **וגם** התיאור שלו קיבלו באותה הרצה חציונים גבוהים חריגים (55
ו-41) - כנראה תנודתיות פנימית של Tesseract בזיהוי גבולות-תיבה בין הרצות שונות על אותה תמונה.
זה לא נפתר ע"י כוונון-סף נוסף (הניסיון הקודם עם `header_min_h` הראה שסף אגרסיבי מדי פוגע
במקרים אחרים) - **המנגנון הנכון להתמודדות הוא `warnings`** (זיהוי-רצף שם-תיאור-שם-תיאור
שבור), לא ניסיון לאפס את שיעור-הטעות ל-0 ע"י כוונון-סף אינסופי.

### קבצים
`ocr_parser2.py` עודכן (לא קובץ חדש) - `lines_to_entries_v2` עכשיו מיישם את זרימת
פסקאות→סיווג-פסקה→אימות-רצף, לא סיווג-שורה-ישיר. `parse_page_v2` מתקן את חישוב גבולות
הפרוסה (כותרת.bottom → כותרת-הבאה.top, לא כותרת.top → כותרת-הבאה.bottom+שוליים).

## גרסה 3 - חקירת "מ"א משה אשר" עד הסוף + כיול אמפירי (לא ניחוש) לסף-הרווח

לפי בקשת המשתמש: לא להסתפק ב"רעש OCR" כהסבר, ולבדוק סטטיסטית.

### נבדק ונדחה: מדידת מרכז (במקום top) לחישוב הרווח בין שורות
המשתמש העלה השערה סבירה: אותיות עבריות מסוימות חורגות מגובה-השורה הרגיל (ל' למעלה; ן,ק,ף,ך,ץ
למטה), כך שמדידת-top או מדידת-bottom עלולות להיות מוטות ע"י צורת-האות עצמה, ולא רק ע"י ניקוד.
ההצעה: למדוד רווח לפי **מרכז-לשורה** (כבר מחושב, `l["center"]`) במקום top-to-top.

**נבדק אמפירית** (עם פונקציית-הקיבוץ האמיתית של הפרסר, לא גרסה גסה) על מדגם מכל 25 העמודים:
רווח-בתוך-פסקה, top-to-top: חציון 46.5, p90=96.0. אותו מדגם, center-to-center: חציון 45.0,
p90=97.0. **כמעט זהה - אין שיפור מעשי**. הסבר סביר: הרעש הדומיננטי בנתונים (מילים מצוטטות
בכוכביות/גרשיים, שגובהן מנופח 40-50%+ מהרגיל) גדול בהרבה מהאפקט העדין של צורת-אות רגילה
(ל' וכו'), ומטביע אותו. **מסקנה: נשארים עם top-to-top**, לא עוברים ל-center-to-center לצורך
מדידת-רווח (center עדיין משמש, כפי שהיה, לקיבוץ-מילים-לשורה עצמו - זה תפקיד שונה).
המשתמש ביקש לראות דוגמה חזותית לרווח-בתוך-פסקה של ~40px, וזה חשף באג במדידה עצמה: הניתוח
הסטטיסטי לעיל (v3) **לא הוגבל לעמודה בודדת** - חלק מ"השורות" שהושוו בפועל ערבבו טקסט מהעמודה
הימנית והשמאלית גם יחד (לדוגמה, שורה "אחת" עם מילים בטווח `left=140` עד `left=1733`, כשגבול
העמודה הוא `964`). זה פסל חלק מהמדגם המקורי.

**אחרי תיקון (מוגבל לעמודה בודדת, כל 25 העמודים):** ההתפלגות שונה משמעותית - חציון 25 (לא 14),
**ואין הפרדה נקייה** בין רווח-בתוך-פסקה לרווח-בין-פסקאות כפי שדווח קודם. אומת חזותית (צילום
מסך): רווח של 41px בין שתי שורות אכן יכול להיות שבירת-שורה תקינה לגמרי בתוך פסקת-תיאור רגילה
(ראה `/tmp/gap40_example.png` - "נ"צ 1963.1337, ...בצפון מערב... / קרדם: קליה ב'; על שם...").

**מסקנה:** סף 55px נשאר בטווח סביר (אחרי רוב-רובם של רווחי-שבירת-שורה, לפני שההצטברות
סביב-כותרות מתחילה), אבל זו **הערכה סבירה, לא כיול מדויק** כפי שהוצג ב-v3. בהינתן חוסר
ההפרדה הנקייה, **מנגנון ה-warnings חשוב יותר משנחשב קודם** - הוא לא "רשת ביטחון" למקרי-קצה
נדירים, אלא כלי מרכזי שצריך להישען עליו, כי אין ערובה סטטיסטית חזקה שהסף עצמו מדויק.

### ניסיון שנכשל: קיבוץ-שורות לפי TOP בלבד (לא מרכז)
לפי הצעת המשתמש - "המרכז חשוב רק לקיבוץ פסקאות-שם", ניסינו להחליף את קיבוץ-המילים-לשורות
הראשי מ-CENTER ל-TOP (עם merge נפרד לתבנית רישא+שם). **נכשל בגדול**: התברר שגם *בתוך* שורה
פיזית אחת אמיתית, ל-TOP של מילים שונות יש שונות של עד ~25px (שוב, בגלל ניקוד) - **באותו סדר
גודל** בדיוק כמו רווח שבירת-שורה אמיתי. אין סף TOP נקי שמפריד בין השניים. **הוחזר לקיבוץ
לפי מרכז**, אבל עם שיפור: **חיתוך (clipping) גובה-מילה** לפני חישוב המרכז (`height_clip_factor
=1.6`), כדי שמילה שקיבלה גובה-OCR מנופח לא "תדחוף" את המרכז שלה לתחום השורה הבאה. זה תיקן את
רוב מקרי המיזוג-השגוי.

### "מ"א משה אשר" - האבחון הסופי (לא נפתר, אך מובן לגמרי)
גם עם חיתוך-הגובה, המקרה הספציפי הזה עדיין נכשל. בדיקה ישירה של הקואורדינטות: המרחק הגולמי
(לא רק הגובה) בין שורת-התיאור הראשונה (מילים עם גובה מנופח 38-53px, בגלל כוכביות/גרשיים
סביב שמות-מקומות מצוטטים כמו `*געתון*`) לבין שורת-ההמשך ("שם חדש", גובה תקין 18-19px) צפוף
במיוחד באופן חריג בתמונה המקורית עצמה (המרכזים המחושבים, גם אחרי חיתוך, נשארים בהפרש 2-3px
בלבד). זו לא עוד "שגיאת חישוב" לתקן, אלא נראה שה-OCR עצמו (Tesseract) מפרש את הפריסה הצפופה
של הטקסט המצוטט-בכוכביות בצורה לא-רגילה. **מסקנה: זה מקרה גבולי אמיתי שדורש בדיקה ידנית**,
לא באג שניתן לתיקון אלגוריתמי נוסף בלי לסכן מקרים אחרים. מנגנון ה-`warnings` (13 מקרים בגרסה
3, שמור בטאב נפרד בקובץ הפלט) קיים בדיוק בשביל זה.

### תוצאה סופית - גיליון 2633 (25 עמ')
**250 רשומות** (אחרי סינון רשומות ללא קטגוריה), 13 אזהרות לבדיקה ידנית. ירידה מ-316 (v2.1) -
זה **שיפור**, לא רגרסיה: v2.1 עדיין כלל כמה זיהויים כפולים/שגויים שתוקנו כאן.

### מוסכמת-שמות קבצים (מעכשיו)
לפי בקשת המשתמש: **לא** להשתמש שוב באותו שם קובץ אחרי תיקון בפרסר - כל פלט מקבל חותמת-זמן
(`booklet_2633_v3_20260825_182532.xlsx`), כדי שיהיה ברור שזו תוצאה טרייה ולא קובץ-cache ישן.

## גרסה 4 - פתרון "מ"א משה אשר" בפועל (לא רק אבחון)

בעקבות בקשת המשתמש לדוגמה חזותית: יצרתי תמונה של האזור בפועל (עמוד 3, טור שמאל, סביב
"מ"א מטה אשר") - ומזה התברר **שהאבחון הקודם היה חלקית שגוי**: חזותית, המרחק בין השורות שם
נראה תקין לגמרי, לא "צפוף באופן חריג" כפי שנכתב קודם.

**האבחון הנכון (בעזרת השוואה מדודה בין הרשומה הבעייתית לרשומה תקינה - "מעלה יוסף"):**
המרחק בין שם לתיאור ב"מטה אשר" (top 172→217, פער 45px) אכן קטן מב"מעלה יוסף" (top 505→564,
פער 59px) - אבל הסיבה האמיתית לתקלה היא ש**הבלוק שנוצר מאיחוד-שגוי של 2 השורות הפיזיות
של התיאור** (בגלל גובה מנופח בכל המילים המצוטטות: `לא.יוזוון`, `מועצות:`, `שלוש`, `*נעמן*`,
`צור*`, `ו"סולם`, `*געתון*` - **7 מילים**, כולן עם גובה 38-53px) יוצר "פסקה בת-שורה-אחת"
מדומה שהחציון שלה (41) עדיין חוצה את סף-השם (32) - **גם אחרי** חיתוך-הגובה.

**התיקון שעבד בפועל:** הוספת מגבלת **אורך** (`max_words=5`) לבדיקת פסקת-שם, בנוסף לגובה.
שם אמיתי הוא תמיד 2-4 מילים; בלוק בן 7 מילים לעולם אינו שם אמיתי, לא משנה מה חציון-הגובה
שלו. זה תפס בדיוק את המקרה הזה בלי להשפיע על שמות אמיתיים.

**תוצאה:** "מ"א משה אשר" מקבל עכשיו תיאור נכון, ו-0 אזהרות בעמוד 3 (היה 1). בסך הכל
במסמך: **246 רשומות, 10 אזהרות** (היה 250/13) - ירידה נוספת בכמות האזהרות מאשרת שיפור אמיתי.

קובץ: `booklet_2633_v4_20260825_183430.xlsx` (230 רשומות אחרי סינון ללא-קטגוריה).

## גרסה 5 - ארכיטקטורה סופית: זיהוי-שם ברמת-שורה ישירה (לא פסקה-לפי-רווח)

### הבעיה שהובילה לשינוי הארכיטקטורה
המשתמש הבהיר את המבנה במפורש: כותרת=ממורכזת+גופן-גדול; בין כותרות - סירוגין קבוע של
פסקת-שם (שורה בודדת, מעט מילים בגודל-בינוני, לפעמים עם רישא קטנה כמו מ"א/ד"נ) ופסקת-תיאור;
**אף פעם לא נצפתה פסקת-תיאור בתחילת עמודה**. הוספת כלל-האימות האחרון הזה חשפה מיד באג אמיתי:
"מ"א אשדות שומרון" (הרשומה הראשונה בגיליון!) נעלמה לגמרי מהפלט. הסיבה: הרווח בין השם לתיאור-
שלו-עצמו נמדד 52px - **מתחת** לסף-הפרדת-הפסקאות (55, מכויל אמפירית) - כך שהשם התמזג עם התיאור
לפסקה בת-2-שורות, נכשל בבדיקת "פסקה בת-שורה-אחת", ונעלם בשקט (כי `cur_name is None` כשמתחילים
בתיאור).

### התובנה: רווח לא אמין מספיק לצומת הקריטי הזה
זה חיזק ממצא קודם (חיפוש הפרדה נקייה בין רווח-בתוך-פסקה לרווח-בין-פסקאות נכשל, ראה גרסה 3-4
למעלה) - אין הבטחה שהרווח בין שם לתחילת-התיאור-שלו-עצמו גדול מהרווח בתוך פסקת-תיאור. **המסקנה:
לא להשתמש ברווח בכלל לצורך ההחלטה הקריטית הזו.**

### הארכיטקטורה החדשה: זיהוי-שם ישיר לפי תכונות-השורה עצמה
`is_name_line(line)`: שורה (כבר מקובצת נכון ע"י `group_words_to_lines`, שכולל חיתוך-גובה
מ-v4) מזוהה כ"שורת-שם" **ישירות**, בלי תלות ברווח שלפניה, אם ורק אם: (א) עד 5 מילים, **וגם**
(ב) חציון-גובה מעל `name_threshold`. `lines_to_entries_v2` פשוט עובר על כל השורות ברצף:
שורת-שם פותחת רשומה חדשה; כל שורה אחרת מצטרפת לתיאור של הרשומה הנוכחית - **ללא שום תלות
ברווח האנכי ביניהן**. שני מנגנוני-ההגנה שכבר קיימים (חיתוך-גובה למניעת מיזוג-יתר של שורות;
הגבלת-אורך-מילים למניעת זיהוי-שווא של תיאורים-עם-מילים-מצוטטות) מספיקים גם בגישה הישירה הזו,
כי הם פועלים ברמת-השורה, לא ברמת-הפסקה.

### שני כללי-אימות חדשים שנוספו (מפרט המשתמש)
1. **שתי שורות-שם ברצף בלי תיאור ביניהן** → אזהרה (היה קיים גם קודם).
2. **עמודה/פרוסה שמתחילה בתיאור (לא בשם)** → אזהרה חדשה. תפסה מיד את הבאג ב"אשדות שומרון"
   לפני שהתיקון בוצע - שימש כאימות-נכונות למציאת הבאג, לא רק כרשת-ביטחון.

### בדיקת מירכוז-לרוחב-עמוד לכותרות (מפרט המשתמש)
נוסף סימן שני, בלתי-תלוי בגודל-אות, לזיהוי כותרות: מרכז-הטקסט-האופקי של השורה חייב להיות קרוב
למרכז-רוחב-העמוד (סבילות 15% מרוחב העמוד). זה עצמאי מבדיקת-הגודל (`header_min_h`) ומקטין את
הסיכוי לזיהוי-שווא של טקסט-גוף גדול שמזדמן להתאים לתבנית-הטקסט של כותרת.

### תוצאה סופית - גיליון 2633 (25 עמ')
**389 רשומות** (אחרי סינון 26 רשומות ללא-קטגוריה מעמ' 1-2), **30 אזהרות** לבדיקה ידנית -
עלייה ממספר-האזהרות הקודם (10), אבל זה **שיפור אמיתי**: הארכיטקטורה הקודמת "בלעה בשקט" רשומות
בעייתיות (כמו "אשדות שומרון" עצמה!) בלי לדווח עליהן בכלל; עכשיו מקרים כאלה מדווחים כאזהרה
במקום להיעלם. מספר-הרשומות עלה (250→389) בעיקר כי רשומות שהיו נעלמות (כמו הרשומה הראשונה בכל
קטגוריה, שוב ושוב לאורך המסמך) עכשיו מזוהות נכון.

קובץ: `booklet_2633_v5_20260826_064431.xlsx`.

## גרסה 6 - סינון סיומות-דף, הערות-שוליים מבודדות, וזיהוי-קו אמיתי מפיקסלים

### סיומות-דף (בעדיפות גבוהה, לפי בקשת המשתמש)
כל עמוד מסתיים בטקסט חוזר: "ילקוט הפרסומים 2633, כ"ו בסיון תש"ם, 10.6.1980" + מספר עמוד, ובעמוד
האחרון גם קולופון ("המחיר 120 אגורות חדשות", "ISSN 0334-2980", "נדפס בדפוס הממשלה, ירושלים").
זה בולען-פוטנציאלי לתוך תיאור/שם של הרשומה האחרונה בעמוד. **נוסף סינון** (`PAGE_FOOTER_RE`,
`COLOPHON_RE`, `BARE_PAGE_NUMBER_RE`) שמזהה ומסיר את כל אלה לפני עיבוד השורות, יחד עם שאר
הסינונים הקיימים (JUNK/SUBHEADER).

### הערת-שוליים מבודדת שהתגלתה כ"נדבקה" לשם: "בְּ`, יְהוְדֶה" → "בי יהודה 3"
נבדק ספציפית לבקשת המשתמש. מקור הספרה "3": מילה בודדת (`left=168`) רחוקה **622 פיקסל** אופקית
מהמילים האמיתיות של השם ("בני"/"יהודה", `left=790-890`) - אך נופלת באותו טווח אנכי (מרכז) בגלל
צירוף מקרים, ומתמזגת בטעות לאותה "שורה" ע"י קיבוץ-המרכז. זו כנראה הפניית-הערת-שוליים (מספר-
מקור ביבליוגרפי), לא חלק מהשם בכלל. **תיקון:** ב-`group_words_to_lines`, אחרי קיבוץ-לפי-מרכז,
נבדק כל מילה מול שאר מילות אותה שורה - מילה קצרה (≤2 תווים, ספרתית) שרחוקה מ-150px+ מהמילה
הקרובה אליה ביותר נחשבת מוצר-לוואי מבודד ומוסרת (`ISOLATION_GAP=150`).

### זיהוי-קו-מפריד אמיתי מפיקסלים (לא רק ניחוש-לפי-רווחי-OCR)
בעקבות שאלת המשתמש "האם הקו האנכי... 'נעלם' מבחינת הפרסר?" ותצלום-עמוד שהראה קו מפריד ברור:
נבדק ואומת שיש קו כזה גם בגיליון 2633 עצמו (סריקת-פיקסלים: עמודה x≈963 עם 1356/2788 שורות
כהות - קו רציף וברור, הרבה יותר מכל עמודה שכנה). **הוחלף `find_gutter_x`**: השיטה הראשית
עכשיו סורקת ישירות את פיקסלי-התמונה (רצועה 40%-60% מרוחב העמוד) ומוצאת את העמודה הכי-כהה;
נופלת חזרה לשיטת-רווחי-OCR הישנה רק אם לא נמצא קו ברור (לפורמטים/גיליונות שאולי אין להם קו
מודפס). זה הרבה יותר יציב מהניחוש הקודם - בבדיקה על עמוד 4, מיקום-הקו הנמדד-מפיקסלים (964)
כמעט זהה לבדיקה הידנית הראשונית (963), לעומת הניחוש-לפי-רווחים הקודם (1166 - **טעות של 200px!**).

### נוספה אזהרת GUTTER_STRADDLE
בדיקת-שפיות: מילה שגבולותיה חוצים את קו-החיתוך שנבחר (לא אמורה להתרחש אם קו-החיתוך נכון) -
מדווחת כאזהרה. **הערה:** גם עם הקו-מבוסס-פיקסלים (מדויק בהרבה), עדיין נמצאו כ-20 מילים חוצות
בעמוד בדיקה אחד - כנראה כי הטקסט המיושר-לשוליים (justified) לא שומר על אותו רוחב-עמודה בדיוק
בכל שורה, כך שקו-חיתוך *קבוע* לאורך כל גובה העמוד לא יכול להימנע לגמרי ממגע עם מילים קרובות-
למרכז בחלק מהשורות. זו תופעה מובנית בפריסה, לא בהכרח שגיאה - **המשתמש טרם החליט** אם לרכך את
הרגישות של האזהרה הזו (לדווח רק על חפיפה משמעותית, לא כל מגע-קצה) - נקודה פתוחה להמשך.

### בדיקה על עמוד 4 (טרם על כל המסמך)
"בְּני יְהוְדֶה" נבדק ישירות: הספרה "3" הוסרה בהצלחה בשתי הרצות (גם עם קו-מבוסס-OCR הישן וגם
עם קו-מבוסס-פיקסלים החדש) - התיקון העיקרי (סינון-מילה-מבודדת) עמיד לשינוי שיטת-הגבול. עם זאת
נשארו כמה שגיאות OCR אחרות בעמוד 4 שלא נבדקו לעומק עדיין (למשל "10" כשם, "במ"א" כשם עצמאי,
"מונאי" כשם) - כנראה עוד שברי-רשומות/גבולות-שגויים שדורשים בדיקה נפרדת.

**טרם הורץ על כל 25 העמודים עם כל התיקונים האלה יחד** - זה הצעד הבא.

### הבעיה שהובילה לשינוי הארכיטקטורה
המשתמש הבהיר את המבנה במפורש: כותרת=ממורכזת+גופן-גדול; בין כותרות - סירוגין קבוע של
פסקת-שם (שורה בודדת, מעט מילים בגודל-בינוני, לפעמים עם רישא קטנה כמו מ"א/ד"נ) ופסקת-תיאור;
**אף פעם לא נצפתה פסקת-תיאור בתחילת עמודה**. הוספת כלל-האימות האחרון הזה חשפה מיד באג אמיתי:
"מ"א אשדות שומרון" (הרשומה הראשונה בגיליון!) נעלמה לגמרי מהפלט. הסיבה: הרווח בין השם לתיאור-
שלו-עצמו נמדד 52px - **מתחת** לסף-הפרדת-הפסקאות (55, מכויל אמפירית) - כך שהשם התמזג עם התיאור
לפסקה בת-2-שורות, נכשל בבדיקת "פסקה בת-שורה-אחת", ונעלם בשקט (כי `cur_name is None` כשמתחילים
בתיאור).

### התובנה: רווח לא אמין מספיק לצומת הקריטי הזה
זה חיזק ממצא קודם (חיפוש הפרדה נקייה בין רווח-בתוך-פסקה לרווח-בין-פסקאות נכשל, ראה גרסה 3-4
למעלה) - אין הבטחה שהרווח בין שם לתחילת-התיאור-שלו-עצמו גדול מהרווח בתוך פסקת-תיאור. **המסקנה:
לא להשתמש ברווח בכלל לצורך ההחלטה הקריטית הזו.**

### הארכיטקטורה החדשה: זיהוי-שם ישיר לפי תכונות-השורה עצמה
`is_name_line(line)`: שורה (כבר מקובצת נכון ע"י `group_words_to_lines`, שכולל חיתוך-גובה
מ-v4) מזוהה כ"שורת-שם" **ישירות**, בלי תלות ברווח שלפניה, אם ורק אם: (א) עד 5 מילים, **וגם**
(ב) חציון-גובה מעל `name_threshold`. `lines_to_entries_v2` פשוט עובר על כל השורות ברצף:
שורת-שם פותחת רשומה חדשה; כל שורה אחרת מצטרפת לתיאור של הרשומה הנוכחית - **ללא שום תלות
ברווח האנכי ביניהן**. שני מנגנוני-ההגנה שכבר קיימים (חיתוך-גובה למניעת מיזוג-יתר של שורות;
הגבלת-אורך-מילים למניעת זיהוי-שווא של תיאורים-עם-מילים-מצוטטות) מספיקים גם בגישה הישירה הזו,
כי הם פועלים ברמת-השורה, לא ברמת-הפסקה.

### שני כללי-אימות חדשים שנוספו (מפרט המשתמש)
1. **שתי שורות-שם ברצף בלי תיאור ביניהן** → אזהרה (היה קיים גם קודם).
2. **עמודה/פרוסה שמתחילה בתיאור (לא בשם)** → אזהרה חדשה. תפסה מיד את הבאג ב"אשדות שומרון"
   לפני שהתיקון בוצע - שימש כאימות-נכונות למציאת הבאג, לא רק כרשת-ביטחון.

### בדיקת מירכוז-לרוחב-עמוד לכותרות (מפרט המשתמש)
נוסף סימן שני, בלתי-תלוי בגודל-אות, לזיהוי כותרות: מרכז-הטקסט-האופקי של השורה חייב להיות קרוב
למרכז-רוחב-העמוד (סבילות 15% מרוחב העמוד). זה עצמאי מבדיקת-הגודל (`header_min_h`) ומקטין את
הסיכוי לזיהוי-שווא של טקסט-גוף גדול שמזדמן להתאים לתבנית-הטקסט של כותרת.

### תוצאה סופית - גיליון 2633 (25 עמ')
**389 רשומות** (אחרי סינון 26 רשומות ללא-קטגוריה מעמ' 1-2), **30 אזהרות** לבדיקה ידנית -
עלייה ממספר-האזהרות הקודם (10), אבל זה **שיפור אמיתי**: הארכיטקטורה הקודמת "בלעה בשקט" רשומות
בעייתיות (כמו "אשדות שומרון" עצמה!) בלי לדווח עליהן בכלל; עכשיו מקרים כאלה מדווחים כאזהרה
במקום להיעלם. מספר-הרשומות עלה (250→389) בעיקר כי רשומות שהיו נעלמות (כמו הרשומה הראשונה בכל
קטגוריה, שוב ושוב לאורך המסמך) עכשיו מזוהות נכון.

קובץ: `booklet_2633_v5_20260826_064431.xlsx`.

### תוצאה על כל 25 העמודים (עודכן אחרי גרסה 6)
**421 רשומות** (399 אחרי סינון 22 ללא-קטגוריה), **52 אזהרות** לבדיקה ידנית. 9 מתוך 11 קטגוריות
זוהו (חסרות: קווי דואר-נע, מחלפים - קטגוריות זעירות, כנראה כותרותיהן לא עברו את סף-הגודל/
מירכוז, טרם נבדק). קובץ: `booklet_2633_v6_20260827_062913.xlsx`.

**הערה:** מספר-האזהרות עלה שוב (30→52) - כמו במעבר הקודם, כנראה משקף גילוי אמיתי של עוד
בעיות קיימות (לא רגרסיה), אך טרם אומת ידנית שזה המצב.

## גרסה 7-8 - חיבור-רשומות בין טורים/עמודים + הצלת שמות-גבוליים

### תיקון: תיאור נעלם כשמתחיל בתחילת טור/עמוד (v7)
28 מתוך 52 האזהרות היו מסוג "טור מתחיל בתיאור" עם קטגוריה אמיתית - נבדק ויזואלית ואושר:
זו **המשכיות אמיתית** (תיאור-רשומה שלא הספיק להיגמר בטור הימני נמשך לתחילת הטור השמאלי, למשל
לפני "אריאל" בעמוד 4) - אבל בקוד הישן זה **נזרק בשקט** (`continue`), מה שחיתך בפועל את התיאור
של הרשומה הקודמת. **תיקון:** `lines_to_entries_v2` מקבל עכשיו `open_entry` ומחזירה
`still_open_entry` - "רשומה פתוחה" עוברת בין טור ימני→שמאלי **באותה פרוסה**, ובין עמוד→עמוד
הבא (רק אם אין כותרת חדשה בעמוד הבא - כותרת תמיד סוגרת רשומה פתוחה). תוצאה: 10 מתוך 28
המקרים האלה נפתרו (השאר כנראה זיהומי-OCR אמיתיים ולא קשורים לחיבור-רשומות).

### תיקון: "הצלת" שמות-גבוליים שנבלעו (v8)
המשתמש ביקש לחפש עוד מקרים כמו "מגילות" (עמוד 3). נבנה כלי-סריקה: איתור רשומות שהתיאור שלהן
מכיל דפוס-פתיחה חוזר ("מועצה המאגדת", "קיבוץ של תנועת" וכו') **פעמיים** - סימן חזק לשם-שנבלע.
**נמצאו 10 מקרים נוספים** (עמודים 4,6,7,9,10,11). בדיקה מדגמית (עמוד 4, "אל-רום"): השם הבלוע
("אללנד") היה `median_h=27.5` - **מתחת** לסף (32), אך רק במעט, ומדובר בשורה קצרה (2 מילים).

**תיקון (`rescue_near_miss_names`):** שורה קצרה (≤4 מילים) שגובהה נופל ב-[0.75×סף, סף) -
כלומר "כמעט עברה" - **וגם** השורה שאחריה מתחילה בדפוס פתיחת-קואורדינטות מובהק (נ"צ/ג"צ/ב"צ
+ ספרה) מוצלת כשורת-שם למרות שהגובה הגולמי שלה נכשל. הרציונל: המשך-תיאור אמיתי כמעט לעולם לא
מתחיל בדיוק בדפוס "נ"צ<מספר>" - רק פתיחת-תיאור של רשומה חדשה עושה זאת.

**תוצאה:** 474 רשומות (מ-421, +53 - הצלה משמעותית של רשומות שהיו נבלעות). 42 אזהרות.

### ממצא חשוב: תיקוני-גבול לא פותרים את בעיית ה"ג'יבריש" הכללית
המשתמש ציין "הרבה ג'יבריש בקובץ" - נבדק כמותית: **90% מהרשומות** (358/399 לפני v7-v8) מקבלות
ציון-ביטחון-מינימלי מתחת ל-40%. אחרי v7-v8: עדיין **87%** (393/452) - **כמעט ללא שיפור
בממוצע**, למרות שתיקוני-הגבול עצמם עבדו נכון ואומתו. **מסקנה:** אלה שתי בעיות נפרדות לגמרי:
(1) דיוק-גבולות (אילו רשומות קיימות ואיפה הן מתחילות/נגמרות) - זה מה שתיקנו כאן, ו-(2) דיוק
ברמת-המילה של Tesseract על הטקסט המנוקד/הפונט הישן הזה - מגבלה בסיסית ונפרדת, שתיקוני-מבנה
לא משפיעים עליה. ציון ה-OCR (עמודות H-J בקובץ) הוא הכלי הנכון להתמודד עם זה - סימון ל-90%
בדיקה ידנית, לא ציפייה לפתרון אוטומטי מלא.

קובץ: `booklet_2633_v8_20260827_131340.xlsx`.

## גרסה 9-10 - הבהרת "ג'יבריש" + תיקון "במ"א" ודפוסים דומים

### הבהרת מונח: "ג'יבריש" ≠ ציון OCR נמוך
המשתמש הבהיר: הכוונה לצרופי-סימנים שלא יוצרים שם/מילה אמיתית בעברית - לא בהכרח קורלטיבי לציון
OCR (רשומה יכולה לקבל ציון גבוה אך עדיין להיות "ג'יבריש" מבחינה זו, ולהפך). זו הגדרה שונה
ונפרדת ממה שנבדק בגרסה הקודמת (התפלגות ציוני-ביטחון).

### תיקון: "במ"א" נוצרה כרשומה בדויה מתוך תיאור "עלי-על"
נבדק ספציפית: "עלי-על" (עם ניקוד) → OCR "אלי-על.. שרס שמ", עם תיאור אמיתי "...בגולן, **במ"א**
גולן; על שם אלי כהן ז"ל...". המילה "במ"א" קיבלה `top=220, h=43.0` כ**שורה מבודדת בת מילה
אחת**, בעוד המילה השכנה "המושבים" (על אותו `top=220` בדיוק!) קיבלה `h=20` - גובה תקין.

### תיקון-על להסבר שחזר על עצמו כמה פעמים: לא "גרשיים מנפחים גובה", אלא רעש-סריקה
בעקבות בדיקה חוזרת (לבקשת המשתמש) על **שני** המקרים שיוחסו ל"ניפוח-גובה ע"י גרשיים/ניקוד"
("אָשַר" בגרסה 4, ו"במ"א" כאן) - שניהם **אינם** באמת עניין של צורת-האות/ניקוד. בזום ישיר על
תיבות-התיחום המדווחות: ב"במ"א" יש נקודה/כתם זר בודד מתחת למילה; ב"אָשַר" (`conf=2.75%` - ציון
ביטחון נמוך במיוחד) יש כמה נקודות-לכלוך מפוזרות מתחת לשורה. בשני המקרים Tesseract **כלל את
הרעש הזה בתוך תיבת-התיחום של המילה**, מה שניפח את הגובה המדווח. **ההסבר הנכון, המאוחד:** זו
לא תופעה שיטתית של "גרשיים/ניקוד מנפחים גובה" (כפי שנכתב בטעות מספר פעמים למעלה) - זו
תופעת **רעש-סריקה מקומי (לכלוך/כתמים בתמונה) שמצטרף בטעות לתיבת-מילה, בדיוק במקומות שבהם
ה-OCR כבר מתקשה לזהות** (ציון-ביטחון נמוך משקף את אותו קושי-בסיסי). התיקונים שבוצעו (חיתוך-
גובה, מגבלת-מילים, `NON_NAME_ADMIN_TOKEN_RE`) עדיין תקפים ועובדים - הם פתרו את התסמין הנכון
(גובה חריג גורם לזיהוי-שם-שגוי), רק ההסבר-לגורם-השורש שתועד קודם היה לא מדויק.

**תיקון (`NON_NAME_ADMIN_TOKEN_RE`):** שורה שהיא **רק** "ב"+קיצור-מנהלי (במ"א/בד"נ, עם/בלי
גרש, עם/בלי ניקוד - נוסף `strip_niqqud()` לצורך ההשוואה) **לעולם** לא יכולה להיות שם - זו
תמיד מילת-יחס באמצע משפט ("ב-מועצה אזורית..."), בניגוד ל"מ"א"/"ד"נ" **בלי** ה-ב', שהן רישא
לגיטימית בתחילת שם. נבדל בבירור מ-NON_NAME: אינו פוסל "מ"א גולן" (שם אמיתי), רק "במ"א" בפני
עצמה.

### תיקונים נוספים לדפוסי-ג'יבריש שנמצאו בסריקה שיטתית (36 מקרים)
- **קואורדינטה כשם** (`PURE_NUMERIC_LINE_RE`): שורה שהיא ספרות/נקודות/פיסוק בלבד (ללא אות
  עברית אחת), למשל "2170.2428." או "1458.0014.. < -" - קטע קואורדינטה שקיבל בטעות מעמד-שם.
- **שבר-כותרת-משנה של מפה** (`MAP_SCALE_FRAGMENT_RE`): "1:100,000):" בלי החלק "מפה מס' X"
  שקדם לו (כנראה נחתך בחיתוך-עמודות) - `SUBHEADER_MAP_RE` הקיים דרש את הביטוי המלא ופספס את
  זה; נוסף דפוס משלים שמזהה גם שבר לבדו.

**תוצאה:** 36 → 23 שמות-חשודים (13 תוקנו). הנותרים ברובם נראים ככשל-OCR אמיתי ובלתי-הפיך על
מילה קצרה בודדת (כמו "לֶ", "ַל", "בֶ") - לא בעיית-מבנה/גבול שניתן עוד לתקן ברמת הפרסר, אלא
מגבלת-זיהוי בסיסית של Tesseract על הטקסט הקשה הזה.

**472→453 רשומות** (סה"כ, אחרי כל השינויים בסבב הזה - חלק מהירידה היא הסרת רשומות-בדויות
שנחשבו רשומה בעבר, לא אובדן תוכן אמיתי). קובץ: `booklet_2633_v10_20260827_150033.xlsx`.

## גרסה 12 - מעבר ל-600dpi (איכות > מהירות, לפי הנחיית המשתמש)

### רקע: הגרש שאבד ב"דרג'ה"
נבדק ספציפית (השם האמיתי: "ואדי דרג'ה"): בדיקת ערכי-האפור הגולמיים (**לפני** כל בינריזציה/ניקוי)
הראתה **אין שום פיקסל כהה כלל** במיקום הגרש - כלומר הגרש אבד כבר בשלב הרינדור מ-PDF לתמונה
(300dpi), לפני שכל קוד-הניקוי שלנו בכלל פועל. אף סף-ניקיון לא יכול לשחזר מידע שלא קיים בתמונה.
**נבדק בפועל:** רינדור ב-600dpi (כפול) - "דרג'ה" מזוהה כמילה אחת שלמה עם הגרש, ביטחון 92.3%.

### כיול-מחדש של סף-הניקיון ב-600dpi
בוצע אותו סוג סריקת-סף (0-400) שנעשתה קודם ב-300dpi, הפעם על תמונות 600dpi. שיא נקי סביב
**150** (לא 4× ליניארי פשוט מ-35, שהיה נותן ~140 - קרוב אך לא זהה; 150 נבדק ואומת ישירות,
כולל וידוא ספציפי ש"דרג'ה" עדיין נשמרת שלמה בסף הזה - בניגוד לסף 200 שגם נראה טוב בממוצע
הכללי אך **הרס** את הגרש הספציפי). `denoise_for_ocr` עודכן לבחור אוטומטית בין 35 (300dpi)
ל-150 (600dpi) לפי רוחב-התמונה בפועל (זיהוי-רזולוציה עצמאי, לא דורש להעביר dpi בנפרד).

### תהליכי-רקע (nohup) לא שורדים בין קריאות-כלי
ניסיון ראשון להריץ את כל 25 העמודים ב-600dpi ברקע (`nohup ... &`) נכשל - **תהליכי-רקע לא
שורדים בין קריאות bash נפרדות** בסביבה הזו (אומת עם בדיקת `sleep` פשוטה). **פתרון:** הרצה
סינכרונית בסבבים (batches) בתוך מגבלת-הזמן לכל קריאה (~250-275 שניות), עם **שמירת-מצב לדיסק**
בין סבבים (`state_600dpi.json`: עמוד הבא, קטגוריה-נשאת, רשומה-פתוחה-נשאת, סף-שם מכויל) -
מאפשר להמשיך בדיוק מאיפה שנעצר גם אם קריאה בודדת נכשלת/עוברת timeout (קרה פעם אחת בפועל,
המצב נשמר תקין עד עמוד 18 והריצה הבאה המשיכה משם ללא אובדן נתונים).

### תוצאה: 600dpi מול 300dpi (כל 25 העמודים, השוואה ישירה)
| מדד | 300dpi | 600dpi |
|---|---|---|
| רשומות עם קטגוריה | 353 | **419** (+19%) |
| אזהרות | 31 | **27** |
| ציון-ביטחון ממוצע (avg_conf) | 76.5 | **79.2** |
| זמן עיבוד לעמוד | ~11s | ~20-40s (פי 2.5-4) |

600dpi משפר בכל הצירים שנמדדו (יותר רשומות אמיתיות, פחות אזהרות, ביטחון גבוה יותר) במחיר
זמן-עיבוד. לפי הנחיית המשתמש (איכות > מהירות במשימה זו), **600dpi הוא כעת ברירת המחדל**.

קובץ: `booklet_2633_600dpi_20260827_212515.xlsx`.

## גרסה 13 - כל 11 הקטגוריות + מנגנון "רשומת שם+קואורדינטות בשורה אחת"

### תיקון 1: כותרות ארוכות מתפצלות ב-600dpi (סקאלה חסרה)
"5. שמות לשמורות טבע" התפצלה לשתי "שורות" נפרדות כי `center_threshold=20` (מכויל ל-300dpi)
נשאר קבוע. **תוקן:** `group_words_to_lines` מחשב `scale = typical_h / REFERENCE_TYPICAL_H_300DPI`
מתוך הנתונים עצמם (לא צריך dpi מפורש), ומכפיל בו את `center_threshold`, `ISOLATION_GAP`,
ופרמטרי `merge_prefix_lines`. תוצאה: שמורות-טבע ועצמים-גיאוגרפיים נמצאו (8/11 קטגוריות).

### תיקון 2: מנגנון "רשומת שם+קואורדינטות בשורה אחת" (משימת "ממשי שמורות טבע")
צילום המסך של שמורות-טבע אישר את התיאור המקורי של המשתמש במדויק: "שמורת-" מופיע **פעם אחת**
בראש כל טור, ואז כל רשומה היא **שורה בודדת**: שם + נ"צ + קואורדינטות, **בלי תיאור נפרד**.
נמצא שזו **תבנית משותפת** לכמה קטגוריות (גם "מאגרי מים": "מאגר X נ"צ Y" - אותו מבנה בדיוק),
לא ייחודית לשמורות-טבע. **תוקן (`split_self_contained_entry`):** זיהוי שורה שמשלבת שם+נ"צ+
קואורדינטות (regex), עם דרישת-גובה **מקלה** (0.55×הסף, לא הסף המלא - כי גופן-השם בקטגוריות
האלה קטן יותר, h=41-43 לעומת סף כללי 57, ככל הנראה כי צריך לדחוס יותר תוכן לשורה) - נבדק
ואומת: מאגרי-מים קפצה מ-0 ל-25 רשומות בעמוד 21; עצמים-גיאוגרפיים, צמתים, דרכים, מערות עלו
משמעותית גם הן (אותה תבנית חלה עליהן חלקית).

### תיקון 3: זיהוי-כותרת מקורב (fuzzy) - ונפילה-חזרה מפני תוכן-עניינים-מקדים
"10. שמות למחלפים" ו"3. קווי דואר נע" (הכותרות עצמן) עברו זיהוי-OCR גרוע מספיק שהמילים
הנדרשות ("שמות", "מחלפים") לא הופיעו כתת-מחרוזת מדויקת. **תוקן:** `fuzzy_detect_header_category`
- דמיון-טקסט (`difflib`) מול רשימת-הביטויים-הצפויים, כנפילה-חזרה כשההתאמה-המדויקת נכשלת.

**אבל:** בדיקה ישירה גילתה שדמיון-טקסט **לא** מבדיל בין כותרת-אמיתית-מעוותת לבין שורות
רשימת-התוכן-העניינים-המקדים בעמוד 2 (שתיהן מקבלות ציוני-דמיון דומים, לעיתים התוכן-המקדים
אף גבוה יותר: 0.79-0.82 מול 0.76-0.81) - וגם בדיקת-המירכוז-לרוחב-עמוד לא עוזרת (כל בלוק-
התוכן בעמוד 2 נופל בטווח-הסבילות כגוש אחד, גם אם כל שורה בודדת אינה "כותרת ממורכזת" במובן
המבני). **הסימן המבחין שכן עבד:** כותרות אמיתיות **לעולם לא** מופיעות 9-11 ברצף על עמוד אחד
(ראינו לכל היותר 3 על עמוד 22) - אם עמוד מניב יותר מ-4 "כותרות" (מוצק+מקורב יחד), זה כמעט
בוודאות תוכן-עניינים-מקדים ולא כותרות אמיתיות; במקרה כזה, ההתאמות-המקורבות (לא המוצקות)
מושלכות לאותו עמוד בלבד.

### תוצאה סופית - כל 25 העמודים, 600dpi, כל התיקונים
**כל 11 קטגוריות נמצאות** (היה 6 בתחילת הסבב הזה): ישובים(195), עצמים גיאוגרפיים(138),
צמתים(92), שמורות טבע(45), אתרים היסטוריים(28), מועצות אזוריות(24), מחלפים(15), מאגרי
מים(7), מערות(6), קווי דואר-נע(5), דרכים(3). **562 רשומות סה"כ** (558 אחרי סינון ללא-
קטגוריה), 44 אזהרות.

### הערה טכנית: תהליכי-רקע לא שרדו, וגם timeout חיצוני יכול "להרוג" באמצע כתיבה
בסבב הזה גם תוקן באג-נלווה: כתיבת-entries הייתה לא-אטומית (נכתבה שורה-שורה תוך כדי עיבוד
עמוד), כך ש-timeout חיצוני שהרג את התהליך **באמצע** עמוד השאיר רישומים חלקיים/יתומים בקובץ
בלי שמצב-ההתקדמות התקדם - איבד עמודים 22-24 בפועל בהרצה אחת. **תוקן:** buffer בזיכרון לכל
עמוד, כתיבה+עדכון-מצב **יחד ואטומית** רק אחרי שהעמוד הושלם במלואו.

קובץ: `booklet_2633_allcats_20260828_062509.xlsx`.

## גרסה 14 - תיקון קריטי: סף-הניקוי (150) פגע באותיות אמיתיות (ה→ר)

### הבעיה שהמשתמש זיהה
"דרך ספר השומרון": התיאור "כינוי ארעי קודם: דרך ארץ **המרדפים**" נקרא "דרך ארץ **רמרדפים**" -
ה-ה' הפכה לר'. באותו עמוד: "ניצנה" הפכה ל"ניצנר" (גם כאן ה'→ר' בסוף המילה). המשתמש חשד
שסף-הניקוי (150, מ-v12) גדול מדי ומוחק חלק אמיתי מהאות ה' (שנבדלת מ-ר' ב"רגל" קטנה נוספת
משמאל).

### בדיקה ישירה (A/B, אותו חיתוך בדיוק, ניקוי כן/לא בלבד)
בהתחלה בדקתי בטעות תמונה שלא עברה כלל בניקוי, וקיבלתי "המרדפים" נכון - מה שהטעה אותי רגעית
לחשוב שהניקוי לא קשור. **המשתמש דחף לבדוק שוב, ובצדק**: כשחזרתי ושחזרתי את **אותו חיתוך-
פרוסה בדיוק** שהצינור האמיתי משתמש בו (לא רק חיתוך-עמודה כללי), ההבדל התגלה נקי לגמרי:
- בלי ניקוי: `'המרדפים;'` (נכון)
- עם ניקוי (סף 150): `'רמרדפים.'` (שגוי)

בדיקת-רכיבים-מחוברים גילתה: לרגל השמאלית המבדילה של ה-ה' יש `area=142` - **קטנה במעט** מהסף
הישן (150), ולכן נמחקה.

### תיקון
סף חדש: **130** (במקום 150). נבדק ואומת ששניהם נשמרים בו: הרגל של ה' (142>130) **וגם**
הגרש ב"דרג'ה" (עדיין תקין). בדיקת-ביטחון-כללית בטווח הזה (130: ממוצע 85.00) כמעט זהה לשיא
הישן (150: 85.47) - הפרש זניח מול מניעת שיבוש-אותיות אמיתי.

**טרם הורץ מחדש על כל המסמך** עם הסף המתוקן - זה הצעד הבא.

### תוצאה סופית - סף 130 + הגנת-fuzzy מתוקנת, כל 25 העמודים
כל 11 הקטגוריות עדיין נמצאות: ישובים(200), עצמים גיאוגרפיים(124), צמתים(94), שמורות טבע(48),
אתרים היסטוריים(26), מחלפים(14), מועצות אזוריות(9), מאגרי מים(6), מערות(6), קווי דואר-נע(3),
דרכים(2). **548 רשומות סה"כ** (532 אחרי סינון), **34 אזהרות** (ירידה מ-44).
אומת ישירות: "המרדפים" ו"ניצנה" קוראים נכון עכשיו (לא "רמרדפים"/"ניצנר").

**הערה:** "מועצות אזוריות" ירדה ל-9 (הייתה 24) - טרם נבדק אם זו רגרסיה אמיתית או תנודתיות-OCR
רגילה בין הרצות; דורש בדיקה בהמשך.

### תיקון נלווה: הגנת-fuzzy השתפרה (ספירת fuzzy-בלבד, לא סה"כ-כותרות)
הגרסה הקודמת של הבאג-בק (">4 כותרות בעמוד") לא הייתה יציבה - בהרצה אחת רק 2 מתוך 11 שורות
תוכן-העניינים-המקדים עברו את סף-הגודל (שונות-מדידה טבעית של OCR בין הרצות), מה שחמק מתחת
לסף-ה-4. **תוקן:** לספור רק התאמות-fuzzy (לא כותרות מוצקות+fuzzy יחד) - 2+ fuzzy באותו עמוד
נחשב לא-סביר בכל מקרה, בלי תלות בכמה כותרות-מוצקות-אמיתיות נמצאות גם כן.

קובץ: `booklet_2633_thresh130_20260828_073345.xlsx`.

### עדכון נוסף (לא תועד בזמנו - מתועד כעת בדיעבד): סף 130→100
נמצא באג **נוסף** מאותה משפחה: "המושבים" (תנועה מיישבת, ברשומות ישובים) נקרא "**רמושבים**"
(ה→ר, אותו סוג-שגיאה כמו "המרדפים"→"רמרדפים"). בדיקת A/B זהה (אותו חיתוך, ניקוי כן/לא) אישרה
שהניקוי אחראי גם כאן: רכיב-מחובר בגודל **117** (קטן אפילו מ-142 שכבר תוקן) הוסר ע"י סף 130.
**סף עודכן ל-100**, ואומת ששלושת המקרים הידועים (המושבים=117, המרדפים=142, הגרש ב"דרג'ה")
נשמרים יחד באותו סף. הקוד הנוכחי (`NOISE_AREA_THRESHOLD_600DPI = 100`) משקף את זה.

## גרסה 15 - פרסר מבוסס-גובה-אותיות (לא גובה-מילים)

### רקע: דיון תכנוני מוקדם שהוביל לכאן
בעקבות באג "הערבה התיכונה"/"חוף עזה" שנעלמו שוב ושוב, נערך דיון-מדידה מפורט (לא שינויי-קוד):
1. גובה **מילה** (TSV) כולל את הרישא (מ"א) בחישוב-החציון, מה שמושך שמות-קצרים כלפי מטה.
2. `tesseract ... makebox` נותן גובה **לכל אות בנפרד**. נבדק על 3 עמודים מגיליון 2633 + עמוד
   אחד מגיליון **שונה** (3165, טיפוגרפיה נקייה, **בלי ניקוד בכלל**, 1985) - בכל המקרים: הפרדה
   נקייה כמעט-מוחלטת בין חציון-אותיות-שם (52-88 / 49-72) לחציון-אותיות-תיאור (24-41 / 38-41),
   **כשלא כוללים את מילות-הרישא (מ"א/ד"נ) בחישוב**. זה מוכיח שהפער לא תלוי-ניקוד - תכונת-פונט
   אמיתית.
3. בדיקה על העמוד **המלא** (בלי חיתוך-עמודות) נכשלה - לא בגלל מהימנות-המדידה, אלא כי קיבוץ-
   שורות (`group_words_to_lines`) ממזג מילים משתי העמודות יחד כשהן על אותו גובה. חיתוך-עמודות
   נשאר הכרחי *לפני* קיבוץ-שורות.

### מימוש (שינוי ממוקד ומינימלי, לפי בקשת המשתמש)
- **`ocr_letter_heights`** (`ocr_parser.py`) - עוטף `tesseract ... makebox`, מחזיר גובה+מיקום
  לכל תו, באותה מערכת-קואורדינטות (top-left) כמו `ocr_tsv`.
- **`group_words_to_lines`** מקבל `box_chars` אופציונלי; כשניתן, `median_h` מחושב מגובה-אותיות
  (לא כולל מילים ששייכות ל-`PREFIX_WORDS`), עם נפילה-חזרה לגובה-מילה-ישן אם לא נמצאו אותיות.
  זה השינוי המרכזי היחיד בזרימת-הנתונים - שאר הארכיטקטורה (זיהוי-כותרות, חיתוך-פרוסות, חוקי-
  שם/תיאור) **לא שונתה**.
- **4 מוקדי-קריאה** עודכנו להעביר `box_chars=ocr_letter_heights(path)` (עמוד מלא לזיהוי-כותרות;
  כל טור בכל פרוסה; כיול-הסף).
- **`calibrate_tier_threshold`** הוחלף מ-Otsu למרכז-הפער-הגדול-ביותר (בטווח 8-70, לא כולל
  כותרות) - Otsu היה רגיש ליחס-הגדלים בין האשכולות ובחר קרוב מדי לקצה אשכול-השמות (50-54,
  לא במרכז-הפער-הריק 45-49), מה שדחה שמות גבוליים אמיתיים (52). מרכז-הפער נותן שוליים שווים
  משני הצדדים.

### שלושה באגים נלווים שהתגלו ותוקנו (קנה-המידה השתנה, סייגים ישנים לא תאמו יותר)
1. **עמוד 2 (תוכן-עניינים-מקדים) חזר** - קנה-המידה החדש (גובה-אותיות) גרם לבלוק-הפתיחה כולו
   (כותרת+הקדמה+רשימה, לא רק פריט-אחד) להימדד מעל הסף. ההגנה הקודמת (ספירת-fuzzy-בלבד ≥2)
   לא תפסה כי חלק מההתאמות כאן **מוצקות** (strict), לא fuzzy. **תוקן:** הוחזרה גם הגנת-ספירה-
   כוללת (>4 כותרות בעמוד → הכל נדחה), **לצד** הגנת-ה-fuzzy הקיימת, לא במקומה.
2. **סף-fuzzy (0.55) תפס טקסט-תיאור אקראי** - "שדות של" (שבר-תיאור סתמי) קיבל ציון-דמיון 0.556
   מול "שמות למערות"/"שמות לדרכים" (בקושי מעל 0.55), וגרם לכותרת-שווא בעמוד 11. נמדד: התאמות
   אמיתיות מקבלות 0.759-0.812 - פער גדול. **תוקן:** `min_ratio` הועלה מ-0.55 ל-0.65.
3. **"שמות לעצמים גיאוגרפיים" מתפצל לשתי שורות** ("שמות לעצמים" / "גיאוגרפיים"), וללא
   "גיאוגרפיים" הטקסט הקצר יותר-דומה בדמיון ל"שמות לצמתים" (מילים משותפות רבות: צ,מ,ת,י,ם)
   מאשר לביטוי-המלא הארוך - גרם לזיהוי-שגוי כ"צמתים" בעמוד 16. **תוקן:** נוסף "עצמים" (בלי
   "גיאוגרפיים") כמילת-מפתח נפרדת ב-`CATEGORY_KEYWORDS`, כך שהזיהוי-המוצק (לא fuzzy) מצליח
   כבר על הצורה המקוצרת.

### תוצאה סופית - כל 25 העמודים, כל התיקונים
**כל 11 הקטגוריות נמצאות. 12/12 מועצות אזוריות בדיוק** (המספר שהמשתמש אישר מהקובץ המקורי) -
כולל שלוש שהיו נעלמות/בעייתיות באופן חוזר בכל הגרסאות הקודמות (הערבה התיכונה, חוף עזה, עמק
יזרעאל, מגילות). **603 רשומות סה"כ** (581 אחרי סינון ללא-קטגוריה), 34 אזהרות. אין עמודים
חסרים, אין זיהום-עמוד-2.

קובץ: `booklet_2633_letterheight_20260829_102614.xlsx`.

## גרסה 16 - זיהוי-קו-מפריד מבוסס-גובה-רכיב-מחובר (לא סריקת-שורה-שורה)

### הבעיה: שיטת "עמודת-הפיקסלים-הכי-כהה" נכשלה על 5/22 עמודי-תוכן
נבדק ישירות (לבקשת המשתמש): עמודים 17, 18, 22, 23, 24 נכשלו בבדיקת-קו-קיים (מתחת לסף 25%
מגובה-העמוד), אך זה **לא** אומר שאין קו - התברר (בעזרת תצלומי-עמוד מהמשתמש) שיש קו אמיתי בכל
עמודי-התוכן, רק שהזיהוי הקודם היה נאיבי מדי (לא התחשב בסיבוב-דף קל, ולא הבחין בין הקו לטקסט
סמוך).

### ניסיון 1 - Hough Transform (נדחה)
זיהוי-קווים גיאומטרי קלאסי, סובלני-לסיבוב. עבד טוב על עמוד 22 (מצא קו בכיסוי 90%+ באזור
מסוים) - אך התברר שזה תפס **סימני-רשומה מקומיים** (קו קטן ליד "מחלף"/"צומת"), לא הקו הראשי -
המשתמש זיהה זאת מתצלום עם עיגולים סגולים. הקו האמיתי (שכבר אימתנו בעבר) הראה כיסוי חלש יותר
בבדיקה הזו - התברר שזה כי אותו "קו מאומת" בעבר הגיע מהשיטה החלופית (רווח-מילים), לא מקו
פיקסלי אמיתי כלל, על עמוד 22 ספציפית.

### ניסיון 2 - בידוד-לפי-רווח-משני-צדדים (עבד חלקית)
לפי הצעת המשתמש: קו-הפרדה אמיתי מוקף ברווח-לבן משני צדדים, בניגוד לאות-בתוך-מילה (חלק מגוש
טקסט צפוף). מומש (`find_isolated_dark_runs_at_row`): ריצת-כהות צרה (≤12px) עם רווח (≥18px)
משני הצדדים. **עבד מצוין** על עמוד 17 (80.2% כיסוי, לעומת 34.4% קודם) ו-18 (58.1%) - אישר גם
שהתזוזה-לכאורה-הקופצת בעמוד 17 הייתה תוצר של דגימה גסה מדי (כל 50px) בשילוב חלון-חיפוש רחב
(200px) שתפס טקסט סמוך; דגימה צפופה (כל 5px) גילתה תזוזה חלקה אמיתית (סיבוב-קל).
**נכשל** על עמוד 24: ניקוד-טקסט-סמוך-לקו יצר דפוס-מקווקו עקבי שגם הוא "מבודד" מספיק לפי
הבדיקה החד-שורתית, ומצא רק 5.5% כיסוי (החמיר לעומת 24.3% בשיטה הישנה!).

### ניסיון 3 - סינון-גובה-רכיב-מחובר (ההצעה שעבדה, פשוטה בהרבה)
**רעיון המשתמש:** לא לבדוק שורה-שורה בכלל - זו בעיה גיאומטרית פשוטה ברמת-העמוד. אם מוחקים
כל רכיב-מחובר שגובהו תואם-אות (כולל ניקוד, מכויל בהרחבה במקומות אחרים במסמך זה - עד ~100px),
נשארים רק עם הקו (רכיב-מחובר ארוך בהרבה מכל אות בודדת). **מומש** (`find_gutter_line_components`,
סף 150px=0.635 ס"מ): `cv2.connectedComponentsWithStats` על רצועה מרכזית, סינון לפי גובה בלבד.
**עבד מצוין על כל העמודים כולל 24** (74.2%, לעומת 5.5% בניסיון 2 ו-24.3% בשיטה המקורית) -
כי הבדיקה לא תלויה כלל בקרבת-טקסט (רק בגובה-הרכיב-עצמו), כך שדפוס-הניקוד-הצמוד בעמוד 24
כבר לא מפריע (כל נקודת-ניקוד היא רכיב-מחובר קטן, לא קרוב לסף 150px).

### בירור עמודים חריגים
- **עמוד 1:** 35.8% כיסוי-שווא - נותר לא-קריטי (0 רשומות בכל מקרה, לא נבדק לעומק).
- **עמוד 25:** 59.9% עם 10 רכיבים ב-X שונים-לגמרי (2437-4621, לא עקבי) - התברר (ויזואלית)
  שזה **לא חלק מהמסמך המקורי בכלל**: עמוד-אישור-סריקה דיגיטלי ("משרד המשפטים", לוגו-מדינה,
  חתימה) שנוסף ל-PDF בתהליך הסריקה המודרני. אין שם קו אמיתי - הכיסוי היה זיהוי-שווא של
  טקסט/חותמת גבוהים-במקרה. תואם לגמרי לממצא-המקורי (0% בשיטה הראשונה).
- **"פערים 10-22px" בין רכיבים בעמוד 24:** אומת ויזואלית - שבירות-דיו קטנות אמיתיות באותו
  קו פיזי (איכות-הדפסה רגילה), לא בעיה מהותית. מגושרים בקלות בשלב-הצירוף.

### כיול-סף: 150px (0.635 ס"מ) נבחר, לא 600px (1 אינטש) שהוצע לשם פישוט
נבדק ישירות: מעבר ל-600px (עגול, פשוט יותר) מפסיד מקטעים-קטנים-אך-אמיתיים בעמודים 22/24
(64.5%→58.8%, 74.2%→69.7%) - טרייד-אוף קטן שהמשתמש בחר לא לקחת; 150px נשאר.

### ארכיטקטורה סופית (`gutter_x_for_slice`) - לפי הצעת המשתמש
**סדר נכון:** קודם חיתוך-לפי-כותרות (כמו תמיד), **ואז** לכל פרוסה בנפרד - ממוצע (משוקלל-
לפי-גובה) של כל רכיבי-הקו שחופפים בטווח-ה-Y של הפרוסה הזו. זה טבעי מבחינה פיזית (הקו עצמו
נשבר בגבולות-קטגוריה, בדיוק כמו הפרוסות) ופותר את בעיית-השיפוע בלי צורך בהתאמת-קו/שרשור
מסובכים. נופל-חזרה לשיטת-רווח-מילים (`find_gutter_x`) רק כשאין אף רכיב חופף לפרוסה נתונה.

### תוצאה סופית - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**. 592 רשומות (569 אחרי סינון), 29 אזהרות.

### נבדק ונשלל: השיפור לא "סוגר" את שאלת ה-fuzzy הפתוחה
נבדק ישירות (בעקבות בקשת המשתמש): "קווי דואר-נע" ו"מחלפים" **עדיין תלויות לחלוטין ב-fuzzy**
גם עם זיהוי-העמודות המשופר - הזיהוי-המוצק עדיין נכשל על שתיהן. זה מראה שקושי-קריאת-הכותרות
האלה אינו נובע מחיתוך-עמודות לקוי - כנראה בעיית-הדפסה/סריקה אמיתית באותם מיקומים ספציפיים.
**המשימה-הממתינה-לאישור (הערכת-fuzzy) נשארת פתוחה ולא נסגרה.**

קובץ: `booklet_2633_componentgutter_20260829_161719.xlsx`.

## גרסה 17 - תיקון GUTTER_STRADDLE: בדיקה לכל-פרוסה מול הערך-האמיתי

### רקע
נבדקה כל אחת מ-18 אזהרות GUTTER_STRADDLE הקודמות באופן פרטני (לבקשת המשתמש) מול `gutter_x_for_slice`
האמיתי (לא `fallback_gutter_x` הישן שבו הבדיקה השתמשה). תוצאה: **אף אחת** לא הייתה מילת-תוכן
אמיתית שחוצה בטעות: 6 כותרות (חוצות מטבען), 2 רעש-שוליים, 1 עמוד-האישור-הלא-מקורי (25),
7 זיהויי-שווא-מתוקנים (נעלמו לגמרי מול הערך הנכון), 2 ארטיפקט-"|" (תו-בודד, כנראה קטע-קו
שנקרא-לא-נכון ע"י ה-OCR-הרגיל, גובה 94-99px, ממוקם ממש על-הקו).

### תיקון
`words_straddling_x` הועבר **לתוך לולאת-הפרוסות** (במקום פעם-אחת-לעמוד-מול-fallback): לכל
פרוסה, נבדק רק המילים שה-`top` שלהן נופל בטווח [s_top, s_bottom) של אותה פרוסה, מול ה-
`gutter_x` **בפועל** שאותה פרוסה נחתכת בו. זה **אוטומטית מוציא שורות-כותרת** מהבדיקה (כותרת
יושבת *בין* פרוסות, לא בתוכן), בלי צורך בסינון-טקסט נפרד.

### תוצאה
מספר-האזהרות **עלה** (18→24 GUTTER_STRADDLE, 29→35 סה"כ) - **לא רגרסיה**: הבדיקה עכשיו
רצה פעם-לכל-פרוסה (לא פעם-לעמוד), כך שכל קטע-קו נפרד מקבל הזדמנות עצמאית "להיתפס" באותו
ארטיפקט-"|" שכבר זיהינו. אומת מדגם מ-4 מהאזהרות-החדשות: 3/4 אותו ארטיפקט-"|" בדיוק, 1/4
רעש-גרפי מעמוד 1 (עמוד-שער לא-אמיתי, כמו עמוד 25). **6 אזהרות-הכותרת ו-7 זיהויי-השווא-
המתוקנים אכן נעלמו כמתוכנן** - התוכן-המהותי של הרשימה נשאר "אותה תופעה, נספרת יותר גרנולרית",
לא בעיה חדשה.

**נותר פתוח:** מקור ארטיפקט-ה-"|" (שברי-קו שנקראים כפיסוק ע"י ocr_tsv) לא טופל ישירות - ניתן
היה לסנן אותו ספציפית (מילה="|", גובה>70, קרוב-לגבול-הפרוסה) אך לא בוצע.

קובץ: `booklet_2633_straddlefix_20260829_204806.xlsx`.

## גרסה 18 - GUTTER_STRADDLE בוטלה (החלטת משתמש)

### רציונל
לאחר תיקון-גרסה-17 (בדיקה לכל-פרוסה מול הערך-האמיתי), נבדק **כל** מקרה מ-24 האזהרות שנותרו:
אף אחת לא הייתה מילת-תוכן-אמיתית שנחתכת - כולן: כותרות (כבר לא מגיעות לבדיקה בפועל אחרי-
התיקון, נעלמו), רעש-עמודי-שוליים (עמודים 1, 25 - לא-תוכן-מקורי), או ארטיפקט חוזר: `ocr_tsv`
(מנוע-הטקסט הרגיל) קורא לפעמים קטע מהקו-המודפס-עצמו כתו "|" (גובה 84-99px, ממוקם בדיוק על
הגבול) - אושר ויזואלית בשלוש דוגמאות נפרדות (עמודים 3, 6, 13 - קו רציף אחד בתמונה, לא תו-
פיסוק אמיתי).

**החלטת המשתמש:** לבטל את הבדיקה. הרציונל: היא לא-אפקטיבית כרגע (אפס-תפיסות-אמיתיות על
הגיליון הזה) בזכות זיהוי-הקו-מבוסס-רכיב-מחובר המדויק (גרסה 16) שהקטין דרסטית את הסיכוי
שמילה אמיתית תיחתך. אם בעתיד (גיליון אחר/פורמט אחר) יתגלה חיתוך-אמיתי-של-מילה, נבין את
הגורם ונתקן נקודתית.

### מימוש
`words_straddling_x` **נשארה מוגדרת בקוד ללא שינוי** (לא נמחקה) - רק הקריאה-בפועל בתוך
`parse_page_v2` הוסרה (הפכה להערה-מתועדת). הפעלה-מחדש עתידית, אם תידרש, יכולה לחזור על
הבלוק המוער במקום לבנות את הלוגיקה מחדש.

### תוצאה סופית
כל 11 הקטגוריות, 12/12 מועצות אזוריות. 592 רשומות (569 בקובץ). **11 אזהרות בלבד** (ירידה
מ-35) - כולן מהסוג המקורי (רצף שם-תיאור שבור), 0 GUTTER_STRADDLE.

קובץ: `booklet_2633_nostraddle_20260829_224254.xlsx`.

## גרסה 20 - שילוב מלא: מכונת-מצבים + מיזוג + ניקוי-מוקדם + regex פשוט למפה

### מה שולב בפועל (כל הפריטים מ"גרסה 19" + תיקונים נוספים שהתגלו תוך-כדי-שילוב)
1. **הסרת שוליים-תחתונים** (5%, `render_page_png`).
2. **`filter_isolated_noise_words`** (גובה-מינימלי + בידוד-אופקי) - מיושם גם ב-`parse_page_v2`
   וגם ב-`calibrate_document_threshold` (תוקן: בהתחלה נשכח מהכיול, מה שגרם לרכיבי-רעש לדלוף
   ולסלף את הסף המחושב ל-20.0 במקום ~32).
3. **מיזוג-חפיפה-ב-Y** ב-`group_words_to_lines`, עם **תיקון-באג קריטי**: הגרסה הראשונה
   קבעה סדר-צירוף-טקסט לפי `left` (מיקום-אופקי) - נכון בתוך שורה בודדת (ימין-לשמאל), אבל
   **שגוי לגמרי** בין שתי שורות-מוערמות-אנכית שמתמזגות (יש להשתמש ב-`top`, מלמעלה-למטה).
   הבאג הזה **הפך שתי שורות-תיאור אמיתיות** (אימות: "המשיך להתקיים..." ו"נ"צ 1905...")
   וגרם לשרשור-שגיאות בכל הרשומה. תוקן. גם `MERGE_GAP` הוקטן מ-8.5×scale ל-3×scale (הערך
   הגדול-יותר, בהקשר עם typical_h גבוה, חצה רווח-פסקה-אמיתי של 17px וגרם למיזוג-שגוי נוסף).
4. **מכונת-מצבים** (FORCE_NAME/FORCE_BODY/CHECK) מחליפה את הבדיקה הישנה `is_name_line`-בלבד
   ב-`lines_to_entries_v2`. **תוקן:** גרסה ראשונה כללה `or is_name_line(...)` כ"רשת-ביטחון" -
   התברר שזו **פוגעת**, לא עוזרת: היא "מצילה" שורות-המשך-תיאור-אמיתיות (כמו "והערבית הקרובה",
   2 מילים, רווח=36<40) שבמקרה עוברות את בדיקת-הגובה-הישנה, וגורמת לאותו שרשור-שגיאות. הוסר -
   ה-CHECK מסתמך על אות-הרווח **בלבד**.
5. **שורת-מפה מאפסת מצב מתוך-הלולאה** (לא מסוננת-מראש) - כדי שאיפוס-אחרי-מפה יעבוד גם כשמצב
   שגוי-קודם הגיע אליה.
6. **`SUBHEADER_MAP_RE` פושט לגמרי**, לפי בקשת המשתמש ("אני בעד ביטויים פשוטים"):
   `r'מ[פש]\S{0,3}\s+\S{0,4}\s+\d'` - מ[פש] (עיוות-OCR של "מפה"), מילה-קצרה, ספרה. **בלי**
   דרישת "ק"מ" בכלל - נבדק שלא נחוץ. אומת (`test_map_regex.py`, מאגר-בדיקה מתויג-ידנית
   `/tmp/combined_test.json`): **8/8 שורות-מפה אמיתיות נתפסות, אפס-שווא מתוך 233 שורות**
   שנדגמו מ-5 עמודים ב-6 קטגוריות שונות (כולל מקרה-מלכודת: תיאור אמיתי המכיל "ק"8 ק"מ").
   שלוש הגרסאות המחמירות-יותר שנוסו קודם (עם "מס"/"קמ" מדויקים) נכשלו על עיוותי-OCR שונים
   שהתגלו בבדיקה ("מס"→"מם", "מס'"→"מסי", "מפ' 9" בלי "מס" כלל) - הגרסה הפשוטה עמידה יותר
   בדיוק כי היא לא מנסה לתפוס טקסט-מדויק.

### תוצאה - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**. **527 רשומות** (511 אחרי סינון). **0 אזהרות**
(היה 11-35 בגרסאות קודמות) - ירידה חדה שדורשת מעקב: ייתכן שמכונת-המצבים אוכפת רצף-תקין
בצורה מובנית מספיק שרוב מקרי-האזהרה-הישנים כבר לא מתרחשים כלל, לא בהכרח שכל השגיאות נעלמו.

### הערת-היקף (לפי בקשת המשתמש)
קטגוריות **7,8,10,11** (מאגרי מים, מערות, מחלפים, צמתים) - בעלות מבנה-שורה שונה (שם+
קואורדינטות בשורה-אחת, `split_self_contained_entry`) - **לא** נבדקו בסבב הזה ועלולות
להכיל טעויות רבות. המיקוד הנוכחי הוא ניקוי-שגיאות בקטגוריות **1,2,3,4,5,6,9** (מועצות
אזוריות, ישובים, קווי דואר-נע, אתרים היסטוריים, שמורות טבע, עצמים גיאוגרפיים, דרכים) -
בעלות מבנה שם-ואז-תיאור הרגיל, שהשילוב הזה מיועד להן.

קובץ: `booklet_2633_gapstate_20260830_135401.xlsx`.

## גרסה 21 - פישוט זיהוי-כותרות: סף-גובה-קבוע במקום מירכוז+הגנות-ריבוי

### רקע - ביקורת-מנגנונים משותפת עם המשתמש
נערכה ביקורת שיטתית על כל המנגנונים בקוד (ראו `MECHANISMS_AUDIT.md`), ובעקבותיה 4 נקודות
יושמו ברצף:

**נקודה 1+2 (זיהוי-קו):** `find_gutter_line_components` עבר לרוץ **ישירות על כל פרוסה חתוכה**
(לא עמוד-מלא ואז שיוך) - `gutter_x_for_slice` בוטלה (הפכה קוד-מת, הוסרה). פרוסות עם
`category=None` מדולגות **לגמרי** - נמצא ונבדק שכל רשומה מהן ממילא נזרקת בבניית-הפלט
(`build_v3_output.py`), ושה-fallback (`find_gutter_x`) שימש רק ב-5/36 פרוסות בכל הגיליון,
**כולן** `category=None` - כלומר עבודה מבוזבזת ב-100% מהמקרים.

**נקודה 3+4 (זיהוי-כותרות):** נמצא (בדיקה ישירה, לא הנחה) שהבעיה-המקורית שדרשה מירכוז+הגנות-
ריבוי לא הייתה כשל-בבדיקת-הטקסט - היא הייתה **סף-גובה נמוך מדי** (`name_threshold*1.10≈50`),
שנתן לתוכן-העניינים-המקדים (עמוד 2, כל 11 השורות + כותרת-המסמך, h=51-57) "להיכנס" לשלב
בדיקת-הטקסט מלכתחילה. נמדדו **כל 11 הכותרות-האמיתיות בגיליון** (כולל 2 שנזקקו ל-fuzzy) -
טווח **70-75, כמעט קבוע**. פער-נקי-לגמרי מול המקסימום-של-תוכן-העניינים (57) - 13 נקודות ריקות.

**מימוש:** `header_min_h` הוחלף מ-`name_threshold*1.10` לערך-קבוע **60** (באמצע הפער-הנקי).
הוסרו: בדיקת-מירכוז (`page_mid`/`center_tolerance`), הגנת-"יותר-מ-4-מועמדים", הגנת-"2+ fuzzy".
`detect_header_category_v2`/`fuzzy_detect_header_category` **נשארות פעילות במלואן** - התפקיד
שלהן השתנה: לא "שומר-שער" (זה תפקיד-הגובה עכשיו), אלא **קביעת-לאיזו-קטגוריה** מבין 11 -
מותר להן להיכשל בבטחה (מחזירות `None`, השורה נדחית בשקט) על כל טקסט-גבוה-אך-לא-קטגוריה
(כמו "רשימת שמות" - כותרת-המסמך-עצמו, h=75, נבדק ונדחה נכון).
גם ניקוי-קוד-נלווה: `hleft`/`hright` בטופל-הכותרות היו נשלפים אך לא נעשה בהם שימוש (רק
לבדיקת-המירכוז שהוסרה) - הוסרו מהמבנה.

### תוצאה - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**. **511 רשומות** (ללא צורך בסינון `category=None`
כלל בשלב-הבנייה - כבר דולג בשלב-הפרסור). **0 אזהרות**. עמודים 1-2 נכונים ל-0 רשומות (היו
תמיד "לא-אמיתיים" מבחינת-תוכן).

קובץ: `booklet_2633_simpleheader_20260830_160230.xlsx`.

## גרסה 22 - חיתוך-שוליים-תחתונים מבוסס-מיקום-footer (לא אחוז-קבוע-מנחש)

### רקע
בעקבות תיקון-הבנה עם המשתמש (ראו "בירור נפרד" בגרסה 19 למעלה): חיתוך-5%-בלבד השאיר את
שורת-הכותרת-התחתית ("ילקוט הפרסומים...") שלמה ב-21/25 עמודים - היא הוסרה בפועל ע"י
`PAGE_FOOTER_RE` (סינון-טקסט נפרד), לא ע"י חיתוך-הפיקסלים. **המשתמש הצביע על כך שזו לא רק
אי-דיוק-בתיעוד אלא הזדמנות-שיפור אמיתית: אפשר ו-**צריך** להסיר את ה-footer כבר ברמת-הפיקסל,
עוד לפני שהוא בכלל מגיע ל-OCR** - צעד ראשון בצינור, לפני אפילו חיפוש-כותרות-קטגוריה.

### מדידה ומימוש
נמדד **מיקום-תחילת-ה-footer עצמו** (לא רק השטח-הריק-מתחתיו) בכל 25 העמודים: טווח
**0.9075-0.9401** מגובה-העמוד-הגולמי. עמוד 24 (הכי-נמוך, 0.9075) נבדק לעומק - התוכן-האמיתי-
האחרון שם מסתיים סביב 0.79, רווח-ביטחון גדול (~660px) לפני שה-footer בכלל מתחיל.

`render_page_png` עודכן: קיטום מ-**0.95 ל-0.90** (מתחת לערך-המינימלי-שנמדד, 0.9075, עם שוליים
קטנים). אומת ישירות: ה-footer נעלם **ברמת-הפיקסל** בשני העמודים-הקיצוניים (24: gutter-safe-
margin הכי-קטן; 16: footer הכי-גבוה, 0.9401) - `ocr_tsv` על התוצאה לא מוצא "ילקוט"/"הפרסומים"
כלל. `PAGE_FOOTER_RE` נשאר בקוד כגיבוי (למקרה של footer שממוקם אחרת מהצפוי), לא הוסר.

### תוצאה - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**, **501 רשומות**, 0 אזהרות. אין ירידה במספר-
הרשומות ביחס לגרסה הקודמת (511→501, ההבדל בטווח-השונות-הרגיל בין הרצות, לא סימן-לאובדן-
תוכן - הועדפה הרצה-קודמת-אחת כדוגמה, לא מגמה שיטתית).

קובץ: `booklet_2633_footercrop_20260830_201942.xlsx`.

## גרסה 22 - עקרון YAGNI: הסרת מנגנוני-footer מיותרים, קיטום מבני במקום טקסטואלי

### רקע
בעקבות בירור (בקשת המשתמש) שהתברר שהכותרת-התחתית **שורדת** את חיתוך-5%-השוליים ב-21/25
עמודים (לא נעלמת ממנו כפי שתועד בטעות בגרסה 18-19) - המנגנון-שבאמת-הסיר אותה מהרשומות
היה `PAGE_FOOTER_RE` (סינון-טקסט נפרד וקיים-מראש).

### שינוי-גישה: קיטום מבני (0.90) במקום הסתמכות-על-סינון-טקסט
`render_page_png` עודכן לקטום ל-**0.90** מגובה-העמוד-הגולמי (במקום 0.95) - נמדד ישירות מיקום-
ההתחלה-של-ה-footer-עצמו (לא רק השטח-הריק-מתחתיו) על כל 25 העמודים: טווח 0.9075-0.9401.
0.90 יושב בבטחה מתחת לכל הטווח - ה-footer כבר לא מגיע ל-OCR בכלל בשום עמוד.

### הסרות (עקרון YAGNI - נבדק אמפירית, לא הונח)
- **`BARE_PAGE_NUMBER_RE`** - הוסר. נבדק: 0 פגיעות בכל 25 העמודים אחרי הקיטום-ל-0.90 (היה
  קיים ספציפית לתפוס שבר-מספר-עמוד שהתפצל מה-footer בשיטה-הישנה).
- **`PAGE_FOOTER_RE`** - הוסר (לבקשת המשתמש, לפי YAGNI - למרות ערך-הכללה-עתידי-פוטנציאלי
  לגיליונות-אחרים, שנשקל אך נדחה). נבדק: 0 פגיעות בכל 25 העמודים.
- **`COLOPHON_RE`** - הוסר. נבדק: 0 פגיעות בכל 25 העמודים (מעולם לא היה קשור ל-footer-
  החוזר, אלא לתוכן-הדפסה-שונה - אך גם הוא לא נמצא בפועל בגיליון הזה).

### תוצאה - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**, **501 רשומות**, **0 דליפות** של טקסט-footer/
colophon לתוך רשומות (מאומת ישירות - נבדק שאין "ילקוט"/"הפרסומים"/"ISSN"/"אגורות" בשום שם-
רשומה). ה-warnings שמורות ריקות (0), תואם לגרסאות האחרונות.

קובץ: `booklet_2633_yagni_20260830_205932.xlsx`.

## גרסה 23 - הסרת ISOLATION_GAP (פוגע יותר משהוא עוזר)

### רקע
נבדק אמפירית על כל 25 העמודים (50 טורים): המנגנון (שנועד לזהות סימני-הערת-שוליים מבודדים
ולהסירם) תפס 5 מקרים אמיתיים בסה"כ, ו-**2 מהם (40%) היו שברי-קואורדינטות-אמיתיות** שנחתכו
(לדוגמה: "9336" הוסר מ-"1572.9336", "2547" מ-"1889.2547") - לא רעש כלל. אושר ויזואלית עם
מלבנים-על-תמונה. בשונה מ-GUTTER_STRADDLE (שהיה "לא-מזיק-אך-חסר-תועלת", 0 תפיסות-אמיתיות),
המנגנון הזה **פעל בפועל ופגע בנתונים אמיתיים** ברוב-המקרים-שהוא-כן-תפס.

### הסרה
`ISOLATION_GAP` **הוסר לגמרי מהקוד** (לא רק הושבת/הוער כמו GUTTER_STRADDLE) - לפי בקשת
המשתמש, כי מנגנון שפוגע-בנתונים-בפועל שונה מהותית ממנגנון-לא-פעיל: אין ערך בשמירתו
כ"אפשרות-להפעלה-מחדש". הוסר גם מ-`group_words_to_lines` וגם ההערה-הכוללת שהזכירה אותו.

### תוצאה - כל 25 העמודים
כל 11 הקטגוריות, **12/12 מועצות אזוריות**. **502 רשומות** (501 בקובץ הסופי). **מאומת ישירות:**
"נ"צ 1572.9336" (צומת קטורה) עכשיו שלמה, לא נחתכת יותר.

קובץ: `booklet_2633_noisofix_20260830_215541.xlsx`.

## גרסה 24 - metadata מבנה-מסמך ב-stage1_input_clean + מנגנון-זיהוי-אוטומטי (שלב ראשוני)

### רקע - צמצום-היקף שגילה בעיה עמוקה יותר
לבקשת המשתמש, צומצם ההיקף-המוצהר של הפרסר לגיליונות "277-3165" (עד הודעה חדשה, לא כולל
4012 החד-טורי). בדיקה ישירה (`calibrate_document_threshold`, ראו "גרסה 21"/דיון-קודם) גילתה
**שגם בתוך הטווח המספרי הזה, 277 ו-971 אינם תואמי-מבנה** ל-2633/3165: 277 הוא **טבלה**
(מספר|שם|קואורדינטות, אומת ויזואלית עמ' 3), 971 הוא **חד-טורי** (שם+תיאור-זורם, לא שתי
עמודות, אומת ויזואלית עמ' 2) - שניהם תואמים לסיווג-הפורמטים-המקורי מתחילת הפרויקט (3
תקופות-פורמט שונות), לא "עוד גיליונות מאותו טווח". מתוך הטווח, **רק 2633 ו-3165 בפועל
תואמים למבנה שהצינור מניח.**

### שלב 1 - עמודות-metadata חדשות ב-stage1_input_clean.xlsx
לפי בקשת המשתמש - **בלי ערכים-קבועים-מראש** (לא enum סגור), כי סף-הפרדה-שם/תיאור עשוי
להיות רלוונטי גם לפורמטים חד-טוריים, לא רק דו-טוריים - הקישור בין העמודות לא-נוקשה.
3 עמודות טקסט-חופשי נוספו: **מבנה העמוד**, **סף הפרדה שם/תיאור (אותיות)**, **שיטת הקביעה**.
מולאו ל-5 גיליונות שכבר נבדקו (2633, 3165, 277, 971, 4012), מבוסס בדיקה-ידנית-ויזואלית +
כיול-בפועל (עבור 2633/3165 בלבד, היחידים עם מבנה-מתאים לכיול).

### שלב 2 - מנגנון-זיהוי-מבנה-אוטומטי (`detect_page_structure`, אבטיפוס)
**גרסה ראשונה** (סימן דו-טורי לפי `find_gutter_line_components` + פער-גדול-בהתפלגות-גבהים
לזיהוי חד-טורי-עם-הפרדה) **נכשלה על 277**: זוהה בטעות כ"חד-טורי עם הפרדת-גובה" - כי גם
בטבלה יש שונות-גובה-קלה (כותרות-פרק כמו "בגליון 8" גבוהות מעט משורות-טבלה) שיוצרת פער-
מספרי-שווא.

**תוקן (`detect_page_structure_v2`):** נדרש שהפער-הגדול-ביותר יהיה **מכפלה גדולה (≥5×) של
הפער-החציוני** בין נקודות-שכנות בהתפלגות - לא רק "הפער-הגדול-מבין-הרבה", אלא **פער-אמיתי-
וריק**, לא רעש-דגימה בהתפלגות-רציפה. נבדק מחדש על כל 5 הגיליונות:

| גיליון | קביעה-ידנית | זיהוי-אוטומטי-v2 | תואם? |
|---|---|---|---|
| 277 | טבלה | טבלה/מבנה-אחיד (gap/median=2.0) | **כן (תוקן)** |
| 971 | חד-טורי (שם+תיאור-זורם) | טבלה/מבנה-אחיד (gap/median=1.0) | חלקי - ראו הערה |
| 2633 | דו-טורי | דו-טורי | כן |
| 3165 | דו-טורי | דו-טורי | כן |
| 4012 | חד-טורי | טבלה/מבנה-אחיד (gap/median=2.0) | חלקי - ראו הערה |

**הערה חשובה על 971 (ואולי 4012):** התווית "טבלה" לא מדויקת-מבחינה-חזותית (971 הוא באמת
שם+פסקת-תיאור, לא טבלה) - אבל **המסקנה המעשית נכונה**: נמצא (נבדק ישירות) ש-`group_words_to_lines`
(קיבוץ-לפי-מרכז-אנכי) **ממזג את שורת-השם עם תחילת-התיאור** לשורה-מקובצת-אחת ב-971 (לדוגמה:
"אָלכז?גור 7. מושב עובדים..." - שם+התחלת-תיאור יחד, h=39 בלבד, לא h=44+ שהיינו מצפים לשם
לבדו) - כך שאין הפרדת-גובה-נקייה **בגישה-הנוכחית**, גם אם בעיצוב-המקורי-של-הדף כן יש הבחנה
חזותית בין שם לתיאור. כלומר: הזיהוי-האוטומטי נכון-לגבי-"האם-הגישה-הקיימת-תעבוד", גם כשהתווית-
המילולית לא מתארת במדויק את מבנה-המקור.

### מצב סופי
- **זיהוי דו-טורי: אמין (3/3, מבוסס `find_gutter_line_components` המאומת-כבר).**
- **הבחנה בין חד-טורי-אמיתי לטבלה: לא הושלמה** - v2 "עובד" במובן שנותן את המסקנה-המעשית-
  הנכונה (העמודה-הזו-לא-מתאימה-לגישה-הקיימת) על כל 5 המקרים, אך התווית-המילולית לא תמיד
  מדויקת (971 לא באמת טבלה). לא סווג-מלא-ומדויק של סוגי-מבנה, רק "האם ניתן להשתמש בגישה-
  הקיימת" - הבחנה גסה-אך-שימושית.
- **תוצאות-האוטומטיות לא נכתבו ל-stage1_input_clean** - רק הערכים-הידניים-המאומתים (שלב 1).
  המנגנון-האוטומטי עדיין לא מספיק-אמין-להזנה-עצמאית של הקובץ-המשותף.

**המשך אפשרי:** לשפר את `group_words_to_lines` להתמודד עם מבנה-מסוג-971 (שם+תיאור-על-אותה-
שורה), או לבנות מנגנון-זיהוי-מבנה נפרד-ומדויק-יותר, לפני שסומכים עליו להזנה-אוטומטית.

## גרסה 25 - חיבור בפועל: run_batch_600dpi.py קורא/כותב ל-stage1_input_clean

### מימוש
בעקבות שאלת-המשתמש "האם יש מקום לשמור את תוצאות-הקליברציה" - התשובה הייתה כן (העמודה
"סף הפרדה שם/תיאור" שכבר נוספה ב"גרסה 24"), אך היא לא הייתה **מחוברת בפועל** לזרימת-העבודה.
חוברה עכשיו: `run_batch_600dpi.py` בודק **קודם** את stage1_input_clean.xlsx (`load_stored_threshold`)
לפני שקורא ל-`calibrate_document_threshold` היקר. אם יש ערך שמור - נטען ומשמש ישירות (0 עלות-
כיול). אם אין - מכייל כרגיל **וכותב את התוצאה בחזרה** (`save_threshold_to_stage1`) עם תיוג
"אוטומטי (calibrate_document_threshold)" בעמודת "שיטת הקביעה" (כדי להבדיל-בבירור מערכים
מאומתים-ידנית, לפי העיקרון שכבר קבענו למניעת-בלבול-עתידי).

### אימות
- **מסלול-קריאה:** נבדק על 2633 בפועל - `"threshold loaded from stage1_input_clean: 45.5"`,
  לא `"calibrated threshold:"` - הכיול היקר (65-100 שניות) נחסך.
- **מסלול-כתיבה:** נבדק **על עותק-זמני-בלבד** (לא הקובץ האמיתי) - ניקוי ערך קיים, כתיבה,
  אימות שהערך-החדש נשמר נכון. הקובץ האמיתי (`stage1_input_clean.xlsx`) אומת כלא-נפגע
  אחרי הבדיקה.

קובץ מעודכן: `run_batch_600dpi.py`.
## גרסה 26 - בנייה-מחדש של הקוד (9.9.2026), אחרי אובדן-הסביבה

הקוד נבנה **מחדש מאפס** לפי המפרט בגוף המסמך (לא שוחזר), ואומת אמפירית מול התוצאות-הידועות.
הקבצים: `ocr_parser.py`, `ocr_parser2.py`, `run_batch_600dpi`, `build_v3_output`.

### ביצועים - שני שיפורים גדולים (מדודים, לא משוערים)

**1. `OMP_THREAD_LIMIT=1` - פי 3.5 מהירות, פלט זהה-בייט-לבייט.**
נמדד (3 סבבים מתחלפים, לא מדידה בודדת, כדי לשלול אפקט-cache): אותה תמונת-טור בדיוק -
ברירת-מחדל **2.7 שניות**, עם `OMP_THREAD_LIMIT=1` **0.78 שניות**, עם `=2` **0.75 שניות**.
כלומר ה-OpenMP של Tesseract **מזיק** בסביבה הזו (תקורת-thread גדולה מהתועלת), לא רק
חסר-תועלת. **אומת ש-הפלט זהה לחלוטין** (`cmp` על TSV ועל makebox - זהים) - זו לא
טרייד-אוף איכות/מהירות אלא שיפור נטו. מיושם ב-`run_tesseract` (`ocr_parser.py`).

**2. עיבוד-עמודים במקביל - פי 2 נוסף (2 ליבות בסביבה הנוכחית).**
סה"כ: הרצה מלאה של 25 עמודים ירדה מ-**~15 דקות ל-2 דקות ו-18 שניות**.

### ארכיטקטורת-המקביליות: עמודים עצמאיים + שלב-חיבור (`reconcile_pages`)
הקושי: בצינור המקורי כל עמוד תלוי בקודמו בשני דברים - **קטגוריה-נשאת** ו**רשומה-פתוחה**
(תיאור שנמשך מעבר לגבול-עמוד). הפתרון (בלי לוותר על אף אחד מהם):
1. **כל עמוד נסרק עצמאית**, עם שני placeholders: `carried_category=PENDING_CATEGORY`
   (סנטינל, כדי שפרוסת-הפתיחה **לא** תדולג) ו-`open_entry=continuation_placeholder(...)`
   (רשומה-פתוחה ריקה עם `name=None`, כדי שמכונת-המצבים תתחיל ב-CHECK ולא ב-FORCE_NAME -
   בדיוק כמו בהרצה טורית שבה עמוד ממשיך תיאור מהעמוד הקודם).
2. `parse_page_v2` מחזירה עכשיו גם **`leading_fragment`** - שורות-הפתיחה שנקלטו לתוך
   ה-placeholder (כלומר: המשך-התיאור מהעמוד הקודם), בנפרד מ-`entries`.
3. **`reconcile_pages`** (ב-`run_batch_600dpi`) עוברת על העמודים **לפי הסדר** ומחברת:
   `leading_fragment` מתמזג לתוך הרשומה-הפתוחה של העמוד הקודם (`merge_fragment`), כל
   `PENDING_CATEGORY` מוחלף בקטגוריה-הרצה, ורשומות שנשארו בלי קטגוריה (עמודים 1-2) נזרקות.

**אומת שהמקביליות שקולה-לטורית**: נבדק ישירות על עמוד 5 - הרצה-טורית (עם רשומה-פתוחה
אמיתית מעמוד 4) נותנת 22 רשומות, מקבילית נותנת 21 + `leading_fragment`; **ההפרש היחיד הוא
לאיזה עמוד משויכת רשומת-הגבול** ("סמת טבעון" מופיעה בתוצאת-עמוד-4 במקום בתוצאת-עמוד-5).
ברמת-המסמך המאוחד: הרשומה מופיעה **בדיוק פעם אחת**, וסדר-העמודים מונוטוני.

### שינויי-מבנה נלווים בריצה
- **שמירת-מצב לפי-עמוד הוחלפה בקובץ-לעמוד**: `scan/pages/page_NNN.json` (כתיבה אטומית).
  זה גם המנגנון שמאפשר המשך-אחרי-הפסקה (`--budget`) - עמוד שכבר קיים לא נסרק שוב, ואין
  יותר צורך ב-`state_600dpi.json` בכלל.
- `--jobs=<n>` שולט במקביליות (ברירת-מחדל 2). **שים לב:** בסביבה עם יותר ליבות יש להשאיר
  `OMP_THREAD_LIMIT=1` לכל worker (זה מה שהקוד עושה) ולהגדיל את `--jobs`, לא להפך.

### מנגנונים חדשים/שונים מהמפרט המקורי (התגלו בבנייה-מחדש)
- **`should_merge_lines`** - מיזוג-שורות מבוסס **חפיפה-אמיתית ב-Y** (≥50% מהגובה הקטן)
  במקום רק "רווח קטן מסף". הרווח-הקטן נשאר כתנאי משני **רק** לשברים זעירים (<0.6×גובה
  טיפוסי - ניקוד/רעש). בלי זה נמדד מיזוג-שגוי של שורות-תוכן נפרדות (עמ' 14: "תל-דן" +
  "חרשת טל" התמזגו לשורה אחת).
- **גיאומטריית-שורה מבוססת ליבת-אותיות** (`top_core`/`bottom_core` = חציון גבולות תיבות-
  ה-makebox של המילה) במקום תיבת-המילה של ה-TSV. זה ההמשך-הישיר של הממצא בגרסה 9-10
  (רעש-סריקה מנפח את תיבת-המילה): מדידת-חציון על האותיות מנטרלת את הניפוח **לפני** חישוב
  המרכז והרווחים, ולא רק בחישוב הגובה.
- **`COORDS_FIRST_RE`** - בקטגוריות שמורות-טבע/מחלפים/צמתים הקואורדינטה מודפסת **לפני**
  השם באותה שורה (אומת ויזואלית, עמ' 14) - לא רק אחריו כפי שתועד ב-`split_self_contained_entry`.
  שתי התבניות נתמכות עכשיו.
- **`is_name_candidate`** - החלטת-שם ב-CHECK מבוססת רווח **וגם** גובה **וגם** סימן-פיסוק
  (שורה שנגמרת בנקודה אינה שם), עם **הצלת-שם-גבולי** (0.75×סף) כשהרווח שלפניה גדול מאוד
  (≥120px). זה מחזיר את תפקיד `rescue_near_miss_names` שהוסר בגרסה 25, בצורה מוכללת.
- **דילוג על עמוד לרוחב** - עמוד 25 הוא עמוד-אישור-סריקה **בפורמט לרוחב** (7084x2377).
  זוהה לפי `page_w > page_h` ומדולג לגמרי (אזהרה `landscape-page-skipped`), במקום להסתמך
  על סינון-תוכן.
- **`filter_isolated_noise_words`** - חישוב-הבידוד מתבצע **רק מול מילים ששרדו את סינון-
  הגובה**. בגרסה הראשונה כתם-רעש שכן שימש כ"שכן קרוב" והציל מילת-רעש אחרת (עמ' 4: שם
  אמיתי נשמט בגלל זה). בנוסף: מילה בת-תו-אחד בלי שכנים כלל נזרקת.

### באג-פלט שהתגלה ותוקן: Excel דיווח "בעיה בתוכן הקובץ"
`build_v3_output` הפיק קובץ ש-Excel סירב לפתוח ("We found a problem with some content...
Do you want us to try to recover"). **הסיבה (אותרה, לא נוחשה):** openpyxl מפרש **כל מחרוזת
שמתחילה ב-`=`** כנוסחה ומכתיב לה `data_type='f'`. שבר-OCR בודד בעמודת "מילה עם הציון הנמוך"
(`'=.ופ'`, רשומה אחת מתוך 553) נכתב לכן כנוסחה - ונוסחה לא-חוקית פוסלת את כל חוברת-העבודה.
זה מסוכן במיוחד בפרויקט הזה כי **הפלט הוא טקסט-OCR משובש מעצם טבעו**: כל תו-פתיחה מוזר
עלול לחזור על עצמו בכל גיליון אחר.
**תיקון (`set_cell`):** אחרי כתיבת ערך מחרוזת, נכפה `cell.data_type = "s"` - הטקסט נשמר
כפי-שהוא ומוצג כטקסט. **אומת:** 0 אלמנטי `<f>` בכל שלושת הגיליונות (בדיקה ישירה ב-XML הגולמי,
לא רק דרך openpyxl), הקובץ נטען ב-LibreOffice ללא שגיאות, וה-Conditional Formatting
המצביע על B1/D1 והכיווניות RTL שרדו. הערך `'=.ופ'` עצמו נשמר בשלמותו.
בנוסף: גופן Arial לכל התאים.

### תוצאה - כל 25 העמודים (9.9.2026)
**553 רשומות**, **12/12 מועצות אזוריות**, **כל 11 הקטגוריות**: ישובים(178), עצמים
גיאוגרפיים(134), שמורות טבע(87), צמתים(84), אתרים היסטוריים(26), מועצות אזוריות(12),
מחלפים(12), מאגרי מים(7), קווי דואר-נע(6), מערות(5), דרכים(2).
523/553 עם קואורדינטות, 104 עם תנועה-מיישבת. **12 אזהרות** (6 `large-gap-continuation`,
5 `gutter-fallback` - כולן בפרוסת-הפתיחה שמעל הכותרת הראשונה, שאין בה קו-הפרדה מודפס,
1 `landscape-page-skipped`).
**אומת ישירות:** נ"צ 1572.9336 (צומת קטורה) שלמה; אפס דליפת-footer/colophon לרשומות;
עמודים 1-2 נותנים 0 רשומות בפלט; אין שמות עם ספרות; אין שמות שנגמרים בנקודה.

### מה עדיין לא אומת בגרסה הזו
- **מספר-הרשומות (553) גבוה מ-501 של גרסה 22-25** - לא נבדק רשומה-רשומה אם ההפרש הוא
  שיפור-אמיתי (רשומות שהיו נבלעות) או פיצול-יתר. שתי הקטגוריות שגדלו הכי הרבה הן
  שמורות-טבע (87 מול 45-48 בגרסאות קודמות) ועצמים-גיאוגרפיים - שתיהן בעלות מבנה
  שם+קואורדינטות-בשורה-אחת שקיבל תמיכה חדשה (`COORDS_FIRST_RE`), **מה שמסביר את הכיוון
  אך לא מאמת את המספר**.
- שתי רשומות עם שם בן-2-תווים ("יד" עמ' 6, "וה" עמ' 8) - כשל-OCR על שם, לא בעיית-מבנה.
- קטגוריות 7,8,10,11 עדיין לא עברו בדיקה שיטתית (אותה הערת-היקף מגרסה 20).
- לא הורץ על 3165 ולא על אף גיליון אחר.

## גרסה 27 - סבב בקשות-משתמש (10.9.2026)

**מכאן והילך, לפי בקשת המשתמש: כל בקשה/שינוי מתועדים כאן**, בסעיף נפרד לכל סבב.

### הבקשות בסבב הזה
1. **ספירה ידנית של המשתמש: 184 ישובים, 27 אתרים היסטוריים** (המשתמש ציין שהמספר עשוי
   לא להיות מדויק). הפלט אז: 178 ו-26.
2. להוסיף לפרסר **בדיקת רווח-אנכי גדול-מהנדרש בין שמות שחולצו** באותה עמודה ואותה קטגוריה,
   כאינדיקציה לדילוג על שם. שאלה: האם זו הבדיקה `large-gap-continuation`?
3. שלושה שמות שלא זוהו כלל, שהמשתמש איתר ידנית: **יונתן** (עמ' 6), **סמר** (עמ' 9),
   **חורבת שרב** (עמ' 13, בסוף הטור השמאלי).
4. **הפלט בלשונית אחת, לפי סדר ההופעה במקור** (להשוואה נוחה מול המקור) - במקום שתי
   רשימות (א ישובים / ב כל השאר) שהוגדרו בסכימה המקורית.
5. שאלה: מה המשמעות של `gutter-fallback`?
6. **פורמט נ"צ הוא nnn.nnn או nnnn.nnnn** - לבדוק לכמה מקומות יש נ"צ בפורמט nnn או nnnn.

### תשובה לשאלה 2: לא, `large-gap-continuation` אינה אותה בדיקה
היא מזהה את **אותה תופעה מהצד השני**: שורת-**תיאור** שהגיעה אחרי רווח גדול (כלומר: השם
שלה לא זוהה, והתיאור נדבק לרשומה הקודמת). היא אכן תפסה את כל 3 המקרים שהמשתמש איתר.
הבדיקה שהתבקשה (רווח **בין שמות**) מומשה בנפרד (`check_name_gaps`), **אך נמצאה חלשה**:
נמדד על כל הגיליון - מרחק-בין-שמות-עוקבים חציון 375px, מקסימום-לגיטימי 708px
(יחס 1.89). דילוג-על-שם מייצר יחס ~2.0 - **חופף כמעט לחלוטין לטווח הלגיטימי**, כי אורך
התיאורים משתנה מאוד מרשומה לרשומה. הסף נקבע ל-2.2 (מעל המקסימום הלגיטימי שנמדד) והבדיקה
מייצרת 0 אזהרות - כלומר היא רשת-משנית בלבד.
**הבדיקה שכן עובדת (חדשה, `possible-skipped-name`):** בפורמט הזה כל רשומה פותחת תיאור
ב-נ"צ. לכן סופרים בכל טור **שורות-פותחות-תיאור מול מספר השמות שנפתחו** - `openers > names`
מצביע ישירות על שם שדולג. זו הבדיקה שאיתרה 3 דילוגים נוספים שאיש לא ידע עליהם (שמורות טבע).

### תשובה לשאלה 5: מה זה `gutter-fallback`
`find_gutter_line_components` מחפש את **קו-ההפרדה המודפס** בין שתי העמודות (רכיב-מחובר
גבוה מ-150px ברצועה המרכזית). כשלא נמצא אף רכיב כזה בפרוסה, הקוד **נופל-חזרה לחצי-רוחב-
העמוד** כנקודת-חיתוך ומדווח `gutter-fallback`. **כל 5 המקרים בגיליון הזה הם פרוסת-הפתיחה
שמעל הכותרת הראשונה בעמוד** (בין ראש-העמוד לכותרת-הקטגוריה) - אזור שאין בו קו מודפס כי אין
בו שתי עמודות-תוכן. כלומר: **לא שגיאה, ואין לו השפעה על הנתונים** (אין שם רשומות). זו
אזהרה שכדאי לצמצם בעתיד לפרוסות-תוכן בלבד.

### שאלה 6: ממצאי בדיקת פורמט הנ"צ + תיקון
**לפני התיקון: 67 מתוך 523 נ"צ לא היו בפורמט nnn.nnn/nnnn.nnnn.** הסיבה העיקרית לא הייתה
שגיאת-OCR אלא **באג-חילוץ**: `COORDS_RE` תפס רק את **החצי הראשון** של הנ"צ כשהמפריד בין
שני החצאים נקרא כ**רווח** (22 מקרים בפורמט nnnn, 14 בפורמט nnn - למשל `נ"צ 0830 0800`
נתן `0830` בלבד), או כ**פסיק** (13 מקרים, `175,275`), או כ**מקף/מקף-כפול** (`209--162`,
`2085-2552` - עיוות-OCR של הנקודה).
**תוקן:** החילוץ תופס עכשיו את שני החצאים עם מפריד רווח/נקודה/פסיק/נקודה-פסיק/מקף (או בלי
מפריד כלל - רצף של 6 או 8 ספרות מפוצל בדיוק באמצע, `211282`→`211.282`), ומנרמל תמיד לנקודה
(`normalize_coords`). **תוצאה: 518 מתוך 534 תקינים, 16 נותרו פגומים** - כולם שגיאות-ספרה
אמיתיות של ה-OCR (`15800.07125`, `1724.200`, `211.;91`), לא בעיית-חילוץ. כולם מדווחים
עכשיו כאזהרת `coords-format` לבדיקה ידנית.
**הערה:** זהו תיקון-חילוץ בלבד. **נורמליזציית-האפסים ל-6 ספרות** (שהוחלטה בסכימה המקורית)
עדיין לא מיושמת - במכוון, כדי לא לשנות ערכים לפני שהחילוץ עצמו נקי.

### שלושת השמות החסרים - האבחון (שלוש סיבות שונות!)
- **יונתן (עמ' 6) ו-סמר (עמ' 9):** ה-OCR קרא את השם כ**תו בודד** (`ת` בביטחון 31%,
  `מֶ` בביטחון 29%) - אומת ויזואלית שהשם מודפס תקין בתמונה. **הכלל שהוסיף אני בגרסה 26**
  ("מילה בת-תו-אחד בלי שכנים נזרקת", שנועדה לנקות ארטיפקט-`|`) **מחקה אותם**. הכלל הוסר.
- **חורבת שרב (עמ' 13):** השם **כן** נקרא, אך מעוות (`| חרנת #זװז־'ב'.`), גובהו נפל מתחת
  לסף, **והוא נגמר בנקודה** - שלושה סימנים שכל אחד מהם לבדו פוסל שורת-שם.
- **מנגנון-התיקון המשותף (`is_garbled_name`):** שורה קצרה (≤4 מילים) ש**השורה אחריה פותחת
  ב-נ"צ** היא שם - בלי קשר לגובה, לפיסוק, או לכמה אותיות ה-OCR הצליח לקרוא. הרציונל
  מבני-ולא-טקסטואלי: תיאור-המשך לעולם אינו נפתח ב-נ"צ. זהו שחזור-מוכלל של
  `rescue_near_miss_names` שהוסר בביקורת-המנגנונים של גרסה 25.

### שלושה שמות נוספים שהתגלו תוך-כדי (המשתמש לא ידע עליהם)
- **יפית (עמ' 6 שמאל), פרי גן (עמ' 10 ימין), קטיף (עמ' 10 שמאל)** - כולם **בראש טור**.
  שתי סיבות:
  1. **שם בראש טור אין לפניו רווח למדוד** (`gap_before=None`), ולכן `is_name_candidate`
     דחתה אותו אוטומטית. תוקן: הכלל המבני (השורה הבאה פותחת ב-נ"צ) חל גם כש-`gap_before`
     הוא None.
  2. **`filter_isolated_noise_words` מחקה שם אמיתי** (`פֶרִי"גן`): הוא היה המילה-האמיתית-
     היחידה בשורתו, וסימן-לכלוך בקצה השני של הטור היה ה"שכן" היחיד שלו - מרחק 1354px
     מתוך 1834 → סווג כמבודד ונמחק. **זו בדיוק אותה פגיעה-בנתונים שבגללה `ISOLATION_GAP`
     הוסר בגרסה 23** (חתך שברי-קואורדינטות אמיתיים) - חזרה בלבוש אחר.
     **תוקן:** בדיקת-הבידוד חלה עכשיו **רק על אסימונים באורך ≤2 תווים** (סימני-הערת-שוליים
     ולכלוך, שבשבילם נבנתה), לא על מילים ממשיות.

### שלוש רשומות-שמורות-טבע שהיו ממוזגות (גילוי של הבדיקה החדשה)
`possible-skipped-name` דיווחה על עמ' 14 שמאל ועמ' 15 ימין. הבדיקה: רשומות שבגוף שלהן
יותר מנ"צ אחד. נמצאו 3 מיזוגים אמיתיים (`עין נוטרה`+`נחל שוח`, `אלון`+`יער ריחן`,
`שער פולג`+`נחל פולג`). **הסיבה:** בקטגוריות בעלות שורה-עצמאית (שם+נ"צ באותה שורה),
פיצול-שורה הותר רק אם הרווח שלפניה > 40px; כאן הרווח היה 38px (שורת-שם-ערבי בסוגריים
לפניה). **תוקן:** שורה ש**פותחת** בנ"צ רשאית לפתוח רשומה ללא תלות ברווח.
שמורות טבע: 86 → 89.

### שינוי מבנה הפלט
`build_v3_output` מייצר עכשיו **לשונית אחת - "רשימת שמות"** ובה כל הרשומות **לפי סדר
הופעתן במקור** (מיון יציב לפי עמוד בלבד; הסדר בתוך העמוד הוא סדר-הפרסור: פרוסה אחר פרוסה,
טור ימני ואז שמאלי, מלמעלה למטה). לשונית "אזהרות" נשארה. **זה גובר על החלטת-הסכימה
המקורית** ("שתי רשימות סופיות: א' ישובים, ב' כל השאר") - ההפרדה לשתי רשימות תיעשה, אם
בכלל, בשלב מאוחר יותר.
**באג-לוואי שתוקן אגב כך:** המיון הקודם היה לפי (עמוד, טור, top) - **שגוי בעמוד מרובה-
פרוסות**, כי הוא ערבב את כל הטורים-הימניים של כל הפרוסות לפני השמאליים.

### המשך (10.9.2026): זיהוי הרווח-הלגיטימי-הגדול-ביותר ושיפור בדיקת-הרווח
המשתמש שאל **בין אילו רשומות** נמצא הרווח-הלגיטימי של 708px. **התשובה: עמוד 19, טור ימני,
בין `נחל תרצה` (ה-OCR קרא "נחל תראָה") לבין `נחל אוג`** (עצמים גיאוגרפיים).
**הסיבה, אומתה ויזואלית:** בין שתי הרשומות יושבת **שורת כותרת-משנה של מפה**
("מפות מס' 12-11 (ק"מ 1:100,000):") יחד עם הרווח-הלבן הגדול שמקיף אותה. אין שם שם חסר.
בדיקה של שאר הרווחים-הגדולים הראתה שזו **תופעה שיטתית**: כותרות-מפה מפוזרות לאורך הגיליון
ומנפחות את המרחק-בין-שמות בדיוק בסדר-הגודל של רשומה חסרה - וזה מה שהפך את הבדיקה לחסרת-ערך.

**שיפור שבוצע:** כל רשומה מסומנת עכשיו בשדה `after_map` (האם קדמה לה שורת-מפה), ו-
`check_name_gaps` **מדלגת על זוגות כאלה**. תוצאה: המקסימום-הלגיטימי ירד מ-1.92x ל-**1.74x**
(462 זוגות במקום 506; 56 רשומות מסומנות `after_map`). הסף עודכן בהתאם מ-2.2 ל-**1.9**.

**מדידת-רגישות (סימולציה, לא הערכה):** נמחקה רשומה בודדת מכל קבוצה בעלת 6+ רשומות
(424 ניסויים) ונבדק אם הבדיקה תופסת את הרווח-הכפול שנוצר: **233 מתוך 424 = 55% זיהוי,
עם 0 התרעות-שווא על הנתונים האמיתיים.** כלומר הבדיקה תופסת בערך **מחצית** מהדילוגים -
שימושית כרשת-משנית, אך אינה תחליף ל-`possible-skipped-name` (ספירת פותחי-תיאור מול שמות),
שתפסה 3 מתוך 3 במקרים האמיתיים. הסיבה למגבלה: אורך-התיאור משתנה כל-כך בין רשומות, שרווח
כפול-מהחציון עדיין נופל לעיתים בתוך הטווח הרגיל.

### תוצאה - כל 25 העמודים (10.9.2026)
**564 רשומות** (היה 553). **ישובים 184** ו**אתרים היסטוריים 27** - **תואם בדיוק לספירה
הידנית של המשתמש**. שאר הקטגוריות: עצמים גיאוגרפיים 137, שמורות טבע 89, צמתים 84,
מועצות אזוריות 12, מחלפים 11, מאגרי מים 7, קווי דואר-נע 6, מערות 5, דרכים 2.
נ"צ: 534 חולצו, 518 תקינים-לפי-פורמט, 16 פגומים (מדווחים).
**27 אזהרות**: 16 `coords-format`, 5 `gutter-fallback` (פרוסת-פתיחה, לא-מזיק), 5
`large-gap-continuation` (4 מהן בעמוד 2 - עמוד-מבוא שאינו תוכן, 1 בעמ' 22 על תו "-"),
1 `landscape-page-skipped` (עמ' 25). **0 `possible-skipped-name`, 0 `name-gap-suspicious`.**

### מה עדיין לא אומת
- שאר 9 הקטגוריות **לא נספרו ידנית** - התאמת 184/27 מאמתת את שתי הקטגוריות שנספרו בלבד.
- שלושה שמות נשארים מעוותים בפלט (`ת`=יונתן, `מֶ`=סמר, `| חרנת #זװז־'ב'.`=חורבת שרב) -
  הרשומה והתיאור נכונים, השם דורש תיקון ידני. ציון-ה-OCR הנמוך מסמן אותם.

## גרסה 28 - הכללת גילוי-הפערים ל"טקסט שלא זוהה", והחלטה על מערך-הבדיקות (10.9.2026)

### הבקשות
1. **להכליל את בדיקת-הפער**: לאתר **קטעי-טקסט גבוהים-מספיק שלא זוהו** (סף TBD) - ממצאים
   כאלה דורשים בדיקה.
2. **לאמת שחיפוש כזה היה מוצא את הרשומות שהיו חסרות.**
3. **להחליט אילו מנגנוני-גילוי הפרסר ישמור ויפעיל.**

### מימוש: `check_unrecognized_text` - בדיקת-כיסוי (coverage)
העיקרון: **כל טקסט שה-OCR קרא בטור-תוכן חייב להימצא בפלט** (כשם או כחלק מתיאור). כל מה
שלא - הוא ממצא לבדיקה. שני מקורות-אובדן נבדקים:
1. **מילים שסוננו** ע"י `filter_isolated_noise_words` (גובה/רוחב/בידוד-אופקי).
2. **שורות שאף רשומה לא צרכה** - כל שורה מסומנת עכשיו `used` (name/body/map/prefix) בזמן
   מכונת-המצבים; שורה בלי סימון היא שורה שנפלה בין הכיסאות.

### כיול הסף (נמדד, לא נבחר-שרירותית)
בסף **גובה≥40 בלבד**: 30 ממצאים - **25 מהם רוחב 2-18px**, כלומר שברי **קו-ההפרדה המודפס**
שנקראו כ-`|`/`\`/`.`/`-` (אותו ארטיפקט שתועד בגרסאות 17-18). הם מטביעים את הממצאים האמיתיים.
**הסף שנבחר: גובה≥40 וגם רוחב≥40** (`AUDIT_MIN_H`/`AUDIT_MIN_W`) - **6 ממצאים לכל הגיליון**,
רשימת-בדיקה סבירה. הסף עדיין תופס שמות-מעוותים-לתו-בודד (רוחבם 146 ו-134 פיקסל), כך שנסיגה
עתידית בסינון תיתפס.

### 6 הממצאים בגיליון 2633 - כולם נבדקו, כולם תקינים
5 מילים-שסוננו (עמ' 1, 6, 9, 10, 11) + שורה אחת שלא נצרכה (עמ' 14, `'5]'` - שבר של
מספר-מפה). **תיקון-הבנה של המשתמש:** הכתמים `'סי'` (עמ' 11) ו-`'וו'` (עמ' 6) **אינם
"דליפה מהעמודה השכנה"** כפי שכתבתי בטעות - **הם ארטיפקטים של הסריקה**, וקו-ההפרדה בין
העמודות מוצק. **הקריטריון שסינן אותם נכון הוא קריטריון-הבידוד-האופקי שכבר קיים** (המשתמש
זכר נכון שהוגדר בעבר): נמדד - `'סי'` במרחק **64%** מרוחב-הטור מהמילה הקרובה באותה שורה,
`'וו'` במרחק **56%**, מול סף של 40%. כלומר: הבדיקה החדשה **חושפת את ההחלטות של הסינון
לביקורת** במקום שיתקבלו בשקט - וכולן היו נכונות.

### אימות: האם הבדיקות תופסות רשומות-חסרות? (ניסוי-נסיגה מבוקר)
כובה כל תיקון בנפרד, ונבדק אילו מנגנונים מתריעים על 4 המקרים הידועים:

| תרחיש | ישויות שאבדו | `unrecognized-text` | `possible-skipped-name` | `large-gap-continuation` | `name-gap` |
|---|---|---|---|---|---|
| כיבוי `is_garbled_name` | יונתן, סמר, פרי גן, חורבת שרב | **לא תפס** | **4/4** | 3/4 | 1/4 |
| כיבוי הגבלת-הבידוד (`ISOLATION_MAX_CHARS`) | פרי גן | **תפס** (h=80 w=205) | תפס | לא | - |

**מסקנה מרכזית:** לבדיקת-הכיסוי יש **נקודה עיוורת מובנית** - כשהשם נבלע לתוך התיאור של
הרשומה הקודמת (במקום להימחק), הטקסט **כן נצרך** ולכן אינו "לא-מזוהה". היא תופסת רק את
המקרה השני: טקסט שנמחק ע"י הסינון. שתי המשפחות משלימות ואינן מחליפות זו את זו.

### כיסוי-הקטגוריות של `possible-skipped-name` (נמדד)
הבדיקה מסתמכת על כך שהתיאור נפתח ב-נ"צ. נמדד לכל קטגוריה כמה מהתיאורים אכן נפתחים כך:
שמורות טבע 100%, אתרים היסטוריים 100%, מחלפים 100%, מאגרי מים 100%, מערות 100%,
ישובים 99%, צמתים 99%, עצמים גיאוגרפיים 57%, ו**שלוש קטגוריות עיוורות לחלוטין (0%):
מועצות אזוריות, קווי דואר-נע, דרכים** (תיאוריהן נפתחים במילים, לא בנ"צ). בשלוש האלה
`large-gap-continuation` הוא הרשת היחידה - וזו הסיבה המרכזית לשמור עליו.

### ההחלטה (משתמש): לשמור את כל ארבעת המנגנונים
| מנגנון | מה תופס | עוצמה |
|---|---|---|
| `possible-skipped-name` | שם שדולג (ספירת פותחי-תיאור מול שמות) | ראשי, 4/4, אך עיוור ב-3 קטגוריות |
| `large-gap-continuation` | תיאור שנדבק לרשומה הקודמת (מצד הרווח) | 3/4, עובד בכל הקטגוריות |
| `unrecognized-text` | טקסט שנמחק ע"י הסינון | המנגנון **היחיד** למשפחה הזו |
| `name-gap-suspicious` | מרחק חריג בין שמות | חלש (1/4 כאן, 55% בסימולציה) - נשמר כרשת-נוספת |

**⚠️ פתוח לבדיקה עתידית (בקשת המשתמש):** להעריך מחדש את **יעילות ארבעת המנגנונים אחרי
הרצה על מספר גדול של גיליונות**. במיוחד `name-gap-suspicious` - עד כה לא הפיק אף ממצא
ייחודי שמנגנון אחר לא תפס; אם גם על מדגם גדול לא יפיק ערך, יש להסירו (עקרון YAGNI שכבר
יושם כאן על GUTTER_STRADDLE ו-ISOLATION_GAP).

### תוצאה - כל 25 העמודים
**564 רשומות** (ללא שינוי), ישובים 184, אתרים היסטוריים 27, שמורות טבע 89.
**33 אזהרות**: 16 `coords-format`, **6 `unrecognized-text`** (חדש), 5 `gutter-fallback`,
5 `large-gap-continuation`, 1 `landscape-page-skipped`, 0 `possible-skipped-name`,
0 `name-gap-suspicious`.
